# 이그니스 정원·승급 규칙 명세서 (RULES.md)

> 코드(`combined_template.html`)에 흩어져 있는 정원·대기·승급 규칙을 한 곳에 정리한 문서입니다.
> 규칙을 추가/수정할 때는 이 표를 먼저 갱신하고, 코드에 반영하세요.

---

## 1. 기본 정원

| 조건 | 정원 | 비고 |
|---|---|---|
| 모든 일정 (기본) | 코트 수 × 6명 | 남녀 구분 없이 총원 기준 |

## 2. 유니온 3번코트 포함 복수코트 (변동 정원)

| 조건 | 정원 | 비고 |
|---|---|---|
| 3코트 중 3번 포함, 확보 전 | 12명 (고정) | "선모집" 개념, 3번코트 없이도 진행 가능한 인원 |
| 3코트 중 3번 포함, 확보 후 | 18명 (코트수×6) | 관리자가 "3번코트 확보 확인" 클릭 시 자동 확대 + 대기자 승급 |
| 2코트 중 3번 포함, 확보 전 | (코트수-1)×8 = 8명 | |
| 2코트 중 3번 포함, 확보 후 | 코트수×6 = 12명 | |

- 적용 조건: `location === '유니온' && court_count > 1 && courts.includes('3번')`
- 확보 상태는 이벤트 인스턴스(날짜)별로 개별 저장 (`court3Confirmed[eventId]`)

## 3. 성별 유연 승급 (2코트 전용)

| 조건 | 결과 |
|---|---|
| 남/여 각 6명까지 우선 모집 | 6명 도달 시 해당 성별은 "성별대기(gender_wait)"로 전환 |
| 3번코트 미확보시 예외 | 정원이 8명으로 줄어있는 동안은 성별 상한도 정원에 비례해서 4명씩으로 자동 축소(정원의 절반), 확보되면 다시 6명씩으로 확대 |
| 판정 시점: 일정 전날 오전 11:50 | 이 시점의 스냅샷으로 최종 결정 |
| 판정 시 소수 성별이 **2명 이하** | 다수 성별이 최대 정원(12명)까지 대기자 순서대로 자동 승급 |
| 대기시간 공정성 상한 | 확대 혜택을 받던 성별이라도, 반대 성별(원래 소수)이 **4명 이상**(자체 복식 가능 수준)으로 늘어나면 그 시점부터 확대를 멈추고 정상 상한(6명, 또는 3번코트 미확보시 4명)으로 되돌아감 — 한쪽만 계속 늘어나서 대기시간이 불공정해지는 것 방지 |
| AI관리자 코트배치 제안 | **(2026-07-24 최종본, 게임수 기반 일반화)** 일정 시작 5분 전 딱 1회, 참석 4명 미만이면 댓글 자체를 안 남김. 각 성별의 **필요 게임수(4명당 1게임)** 를 계산해서 판단. **규칙**: ① 한쪽 성별만 있으면 4명↑ "OO복" / 4명↓ "OO단식" ② 한쪽이 정확히 6명이고 반대쪽도 4명 이상이면 "남복1코트, 여복1코트" ③ 한쪽만 4명 이상이면 "{다수}복, 혼복" (2코트+다수가 8명 이상이면 "{다수}복,혼복,{다수}복 순서로 3교대") ④ 양쪽 다 4명 이상(6명 케이스 제외): 둘 다 2게임 이상 필요하면 "남복1코트,여복1코트" / 게임수 합이 3 이상이고 한쪽만 1게임이면 "{게임적은쪽},{게임많은쪽}...순서로 N교대"(예: 남5여9→"남복,여복,여복 순서로 3교대") / 그 외는 "남복,여복" ⑤ 양쪽 다 4명 미만이면 "혼복" — 항상 "~하시면 좋을 것 같아요. 즐테하세요! 🎾"로 부드러운 추천 톤 마무리, 받침없는 단어(교대,코트) 뒤 조사는 "로" 자동교정 |

## 40. 알림 읽음처리 (회원별 추적)

| 항목 | 규칙 |
|---|---|
| 기존 버그 | `read`(boolean) 단일 플래그가 전체 회원 공용이라, 한 명이 읽으면 다른 회원 알림까지 전부 읽음 처리되던 문제 |
| 수정 | `read_by`(회원 이름 배열) 컬럼으로 회원별 추적, "안읽음 카운트" = 그 배열에 내 이름이 없는 것들 |
| DB 마이그레이션 | `supabase_add_notification_read_by.sql` 실행 필요 (컬럼 추가) |
| 게임형식 소프트 제안 | 순수 동성복식(여복/남복)일 때만: 대기없이 딱 4명이면 "정규 6게임 풀게임" / 대기인원 있으면 "총합 8점 게임(빠른진행) 추천, 의무 아님" / 그 외 애매한 인원 구성은 "코트 상황에 맞게 유연하게 진행해 주세요"로 단순 안내. 혼합복식이거나 인원 구성이 애매하면 형식 제안 자체를 생략, 인원정리만 |
| 마감 표시 | 여자참석마감/남자참석마감/선마감/전체취소 전부 전체 회원에게 보이는 큰 배너로 표시, 처리시 AI관리자 댓글도 함께 남김 |
| 판정 시 양쪽 다 3명 이상 | "균형" 상태로 유지, 6:6 유지 — **단, 남/여 확대 결정과 달리 영구 고정되지 않고 매 주기 재검토됨** (이후 소수 성별이 2명 이하로 줄어들면 그 시점에 확대 결정으로 전환) |
| 강제 취소 | 없음 — 소수 인원 자진 취소는 본인 판단 (시스템이 강제하지 않음) |

- 남/여 확대 결정은 한 번 내려지면 영구 고정된다 (`genderFlex[eventId]`). "균형" 상태는 영구 고정이 아니며, 상황이 바뀌면(예: 소수 성별이 나중에 2명 이하로 줄어듦) 이후 주기에 재검토되어 확대 결정으로 전환될 수 있다.
- 승급 시 "🤖 AI관리자" 명의로 해당 일정 댓글에 사유 자동 기록
- ⚠️ **다른 정원 확대 요인(3번코트 확보, 관리자 정원 조정 등)이 생겨도, 성별당 6명 상한은 11:50 판정 전까지 유지된다.** 즉 3번코트가 일찍 확보되어 전체 정원이 늘어나더라도, 이미 6명에 도달한 성별은 그 시점 이후 대기자가 곧바로 자동 승급되지 않고 11:50 판정을 기다린다.

## 4. 관내:관외 비율 (종합운동장 전용)

| 총 참석 인원 | 관외 허용 인원 |
|---|---|
| 4명 이하 | 1명 |
| 10명 이하 | 2명 |
| 그 이상 | 코트당 2명 (코트수×2 절대 상한) |

- "관외마감" 배지는 **코트수×2 절대 상한 도달 시에만** 표시
- 비율 미충족 관외 신청자는 거절되지 않고 "관외대기(gwae_wait)"로 등록
- 관내 인원이 채워지는 즉시, 원래 신청 시각 기준으로 자동 승급

## 5. 참석 마감 & 긴급 요청

| 시점 | 동작 |
|---|---|
| 시작 1시간 전부터 | 참석 변경·취소 잠금 (지난 일정 포함) |
| 마감 후 참석 시도 | "긴급 참석 요청(late_request)" 큐로 전환, 운영진 승인 필요 |
| 운영진 승인 | 최종 참석 확정 |

## 6. 관리자 선마감

| 상태 | 동작 |
|---|---|
| 선마감 처리 | 이후 신규 참석 시도는 정원과 무관하게 "선마감대기(preclose_wait)"로 자동 등록 |
| 기존 참석자 | 그대로 유지 (취소되지 않음) |
| 자동승급 | 선마감 중에는 전면 정지 (해제 전까지 아무도 자동 승급되지 않음) |
| 해제 | 정상 정원·자동승급 로직 복귀 |

## 7. 주말 가족&지인 초청코트

| 항목 | 규칙 |
|---|---|
| 적용 요일 | 토요일·일요일만 (`dow === 0 \|\| dow === 6`) |
| 설정 단위 | 개별 일정(날짜) 단위, 템플릿 전체 아님 |
| 코트 배분 | 관리자가 지정한 N개 코트만큼 일반 정원에서 차감 |
| 일반 정원 | (코트수 - N) × 6명 |
| 초청코트 정원 | **항상 4명 고정** (N값과 무관), **정회원 본인 참석 포함** |
| 참석 방법 | 정회원 본인이 "🎾 나도 초청코트로 참석" 버튼으로 본인 참석 체크(1명) 후, "+ 동반 가족·지인 추가"로 나머지 인원(최대 3명) 추가 |
| 화면 위치 | 여자/남자 참석 박스 바로 아래 별도 섹션으로 표시 |
| 표시 방식 | 성별 구분 없이 "정회원/게스트" + 게스트는 초청한 회원 이름도 함께 표시 |
| 독립성 | 초청코트 인원·대기는 일반 정원과 완전히 분리 계산 |
| ⚠️ 데이터 저장 | `is_invite`/`promoted_from`/`invited_by`/`display_name` 필드는 `ignis_votes` 테이블에 별도 컬럼 필요 (마이그레이션 SQL 실행 필수) |

## 8. 히든 운영진석

| 항목 | 규칙 |
|---|---|
| 대상 | 지정된 스태프 5인 |
| 발동 조건 | 일반 정원(max_players) 초과 시, 스태프에 한해 추가 참석 허용 |
| 카운트 영향 | 성별 카운트(female/male)·정원 계산(regularTotal)에는 **포함되지 않음** (순수 오버플로우) |
| 표시 방식 | 남/여 참석 리스트 안에 함께 표시하되, 골드톤 색상 + "★히든" 태그로 구분 |
| 성별당 1자리 제한 | 스태프 본인이 직접 참석 눌러서 히든 등록하는 경우 **성별당 1자리로 제한** — 이미 같은 성별 히든석이 있으면 관리자모드로 직접 대기자를 이동하라고 안내. **관리자모드로 타인을 대기→참석 이동시키는 건 이 제한과 무관하게 계속 가능** |

## 9. 취소 통계

| 항목 | 규칙 |
|---|---|
| 카운트 대상 | 본인 취소로 인해 **복식(4인)이 무산되는 경우만** |
| 카운트 제외 | 대기 인원이 있어 즉시 대체되는 일반 취소 |
| 집계 방식 | 회원별 누적 횟수 (단일 숫자, 세부 사유 없음) |
| 노출 | 주간 결산 보고에 포함 |

## 10. 결산 보고 (하루 1회 통합)

| 항목 | 규칙 |
|---|---|
| 트리거 시점 | **매일 오후 12:04 딱 한 번** (관리자 로그인 여부와 무관, 고정 시각) |
| 발동 방식 | 알림 로그에 1건 기록 (팝업 아님) — 관리자 로그인마다 반복되던 중복 문제 해결 |
| 포함 내용 | 오늘 전체 일정의 참석/대기/불참 현황, 비상 상황 요약 (페널티 등 개인정보 전혀 포함 안 됨) |
| 공개 범위 | **전체 회원 공개** — 알림으로도 뜨고, 오늘현황 패널 하단에도 실시간 표시됨 |
| 비상 표시 조건 | 총 참석 4명 이하 **또는** 한쪽 성별만 있는데 4명 미만 (복식 불가) |
| 비상 표시 제외 | 새벽테니스(06:00-08:00) — 원래 상시 소수 인원이라 제외, **참석·대기·불참 전부 0명인 빈 일정도 제외** |
| 중복 방지 | 발송 직전 서버에서 재확인 후 발송 (여러 세션 동시 체크로 인한 중복 발송 방지) |
| 그 외 항목 | 긴급 참석 요청, 코트 마감, 자동승급 등은 결산 보고에 안 넣고 **발생 즉시 개별 알림**으로 처리 |

---

## 11. 승급 표시 아이콘

| 상황 | 표시 |
|---|---|
| 연속참석(연참) 대기에서 자동/승인으로 참석 전환 | 🔁 연참승급 |
| 그 외 대기(일반·성별·관외·관외승인·긴급승인·선마감)에서 참석 전환 | ⬆️ 대기승급 |
| 처음부터 바로 참석 체크한 경우 | 표시 없음 |

## 12. 자동화: 3번코트 확보 확인 팝업

| 항목 | 규칙 |
|---|---|
| 발동 조건 | 오늘 일정 중 3번코트 포함 + 아직 미확보인 일정이 있을 때 |
| 발동 시점 | 오전 11시 이후, 관리자 모드 접속 시 |
| 동작 | "확보되셨나요?" 확인 팝업 → 예: 3번코트 확보 버튼 자동 실행(정원 확대+대기자 승급+알림) / 아니오: 그날 다시 안 물어봄 |
| 반복 방지 | 일정당 하루 한 번만 (브라우저 기준) |
| 참석→대기 안내 | 회원이 참석 눌렀는데 3번코트 미확보로 정원마감이라 대기 전환된 경우, "3번코트 미확보라 확보 전까지 N명만 우선모집 중, 확보되면 자동 승급됩니다"라고 구체적 사유를 팝업으로 안내 |

## 13. ⚡ "지금 자동승급 실행" 버튼 (관리자 수동 즉시실행)

| 항목 | 규칙 |
|---|---|
| 위치 | 모든 일정 카드, 관리자 전용 |
| 확인창 | "지금 기준으로 실행하시겠습니까? (현재 남X·여Y·대기Z)" |
| 2코트 일정 | 성별 유연승급 미판정 시 지금 시점 기준 즉시 판정(확대/균형) + 확대 결정된 성별 대기자 승급 |
| 연속참석 대기 | 정오(12시)까지 기다리지 않고 자리 있으면 즉시 승급 |
| 관외대기 (종합운동장) | 함께 즉시 승급 시도 |
| 시간 제한 | 없음 — 관리자가 언제든 강제 실행 가능 |

## 14. 당일취소 카운트 & 경고

| 항목 | 규칙 |
|---|---|
| 판정 기준 (OR) | ① 시작 3시간 이내 취소 <br> ② 본인 취소로 복식(4인) 불가능 |
| 대상 | 본인이 참석 체크했다가 다시 눌러서 취소하는 경우만 (신규 불참/대기 등록은 해당 없음) |
| 카운트 | 회원별 누적 (기존 "복식무산 유발 취소"와는 별개의 카운터) |
| 본인 안내 | 취소 즉시 팝업 — "당일취소 하셨습니다. 누적 N회 (3회 이상 시 운영진 안건 상정 및 페널티 가능 안내)" |
| 관리자 보고 | 즉시 알림 + 주간 결산 보고에 누적 집계 포함, 3회 이상은 강조 표시 |
| 페널티 방식 | 미정 (현재는 카운트·경고만) |
| **공개 범위** | 당일취소·복식무산 유발 취소·수동 경고·페널티 부여/해제 관련 알림은 **관리자와 본인만** 확인 가능 (`category:'warning'`) — 다른 일반 회원에게는 노출되지 않음, eventId가 있어도 예외 없이 비공개 |
| **경고현황 통합** | 당일취소·복식무산 유발 취소 둘 다 통합 경고이력(`warnings`)에 자동 등록되어, 알림 벨 왼쪽 "⚠️ 경고현황 퀵패널"에서 관리자만 확인 가능 |

## 15. 보안 — PIN 해시화 & RLS 강화

| 항목 | 규칙 |
|---|---|
| 회원 PIN 저장 방식 | SHA-256 해시로 저장 (관리자 비밀번호와 동일 방식) |
| 기존 평문 PIN | 폐기하지 않고 유지 — 다음 로그인 성공 시 자동으로 해시로 업그레이드 (마이그레이션 안전장치) |
| 관리자 화면 표시 | 실제 PIN 값 대신 "설정됨 🔒" / "미설정"만 표시 |
| RLS 강화 | `supabase_harden_rls.sql`로 불필요한 삭제 권한 제거 (제한적 강화, 완전한 인증 방어 아님) |
| 실제 세션 기반 보안 | Supabase Auth 마이그레이션 필요 — 장기 과제로 별도 계획 |

## 16. 비회원 제한된 보기 & 비밀번호 정책

| 항목 | 규칙 |
|---|---|
| 비회원(로그인 전) 노출 | 일정 제목·날짜·시간·코트 정보, 정원/3번코트 상태 배지만 노출 |
| 비회원 비노출 | 참석/불참/대기 명단, 댓글, 하단 네비게이션 전체 |
| 신규 회원 초기 비밀번호 | **0001로 통일** (기존엔 직접 설정) |
| 비밀번호 변경 | 우측 상단 "🔑 비밀번호 변경" — 현재 비밀번호 확인 후 4자리로 변경 |

## 17. 참석 통계 대시보드 & 대기 순번 안내

| 항목 | 규칙 |
|---|---|
| 위치 | 랭킹 탭 하단 |
| 표시 내용 | 선택한 기간(주간/월간/분기) 기준 요일별·시간대별 참석 인원 막대그래프 |
| 대기 순번 | 본인이 대기 중인 일정 카드에 "OOO 대기 N번째" + 승급 조건 안내 표시 |

## 18. 초청코트 관리자 이동권한 & 단독마감

| 항목 | 규칙 |
|---|---|
| 이동권한 | 일반 참석자 ↔ 초청코트 상호 이동 가능 (이름칩 옆 "이동▾" 드롭다운) |
| 게스트 | 초청코트 게스트는 삭제만 가능(정회원 아니므로 "일반참석으로" 옵션 없음) |
| 단독마감 | "🚫 초청코트만 마감" — 일반 코트 선마감과 완전히 독립적으로 작동, 이미 참석 중인 사람은 마감 중에도 취소 가능 |

## 19. 알림 구독 설정

| 항목 | 규칙 |
|---|---|
| 위치 | 알림 패널 상단 "⚙️ 알림 설정" (접이식) |
| 끌 수 있는 항목 | 자동 승급 알림 / 코트 상태 변경 알림(3번코트·선마감·초청코트 등) |
| 항상 표시 | 본인 참석/불참/대기, 댓글 등은 끌 수 없음 |
| 저장 위치 | 브라우저별 로컬 저장 (기기마다 개별 설정) |
| 개별 삭제 (관리자 전용) | 알림 패널의 각 항목에 "삭제" 버튼 — 서버에서 실제로 삭제되어 **모든 회원의 알림 목록에서도 사라짐** |

## 19-1. 일정 카드 표시 규칙 (시간대 배지 / 요일 색상 / 코트 강조 / 자동 접기)

| 항목 | 규칙 |
|---|---|
| 시간대 구분 배지 | 카드 우측 상단, 시작 시각 기준: 06~09시 "새테" / 10~13시 "오전" / 14~17시 "오후" / 18시 "저녁" / 19시~ "밤" |
| 요일+시간 글자색 | 평일(월~금) 검정 / 토요일 파랑 / 일요일 빨강 |
| 코트 번호 강조 | 코트 번호(예: "3번 코트") 부분만 별도로 초록색 강조 표시 |
| 자동 접기 | 종료 시각으로부터 **20분이 지난 일정**은 자동으로 접힌 상태(⏱️ 요약 한 줄)로 표시, 클릭하면 펼쳐짐 — 새벽테니스만이 아니라 **모든 지난 일정에 공통 적용** |
| 새벽테니스 자동승급 예외 | 일반 일정은 "당일 낮 12시"부터 자동승급 체크하지만, 새벽테니스(06:00-08:00)는 그 시각이면 이미 끝나있어서 **전날 오후 11:50부터 시작 직전까지**로 예외 처리 |

## 20. 토요일 저녁 → 일요일 3번코트 확보 부탁 알림

| 항목 | 규칙 |
|---|---|
| 발동 조건 | 오늘이 토요일이고, 내일(일요일) 3번코트 포함 일정이 아직 미확보일 때 |
| 발동 시점 | 토요일 각 세션(1부/2부) **시작 1시간 전** |
| 대상 | 그 세션에 참석 체크한 회원 전원 (개인 알림) |
| 문구 | "내일(일요일) 일정 3번코트가 아직 미확보예요, 확인 부탁드려요" |
| 중복 방지 | 세션당 하루 1회 |

## 21. 여자/남자 참석 개별 마감

| 항목 | 규칙 |
|---|---|
| 위치 | 일정 카드, 관리자 전용 "🚫 여자참석 마감" / "🚫 남자참석 마감" 버튼 (독립 토글) |
| 동작 | 마감된 성별로 신규 참석 시도 시 자동으로 대기 등록 |
| 독립성 | 여자/남자 각각 따로 켜고 끌 수 있음, 전체 선마감과도 무관하게 독립 작동 |
| 배지 | 수동마감 시 "🚫수동마감" (기존 인원 6명 도달 시 자동 표시되는 "🔒마감"과 구분) |
| 예외 | 관리자는 마감 중에도 강제 배정 가능 |

## 22. 관리자 페널티(참석금지) & 경고조치 시스템

| 항목 | 규칙 |
|---|---|
| 페널티 | 관리자가 회원에게 참석금지 기간 부여 (1주일/2주일/1개월/3개월 또는 직접 일수 입력) + 사유 |
| 페널티 중 | 해당 회원은 어떤 일정이든 신규 참석 시도 시 차단, 해제일까지 안내 팝업 표시 |
| 페널티 해제 | 관리자가 언제든 조기 해제 가능 |
| 경고조치 | 관리자가 회원에게 경고 부여 — 사유 4종(당일취소/무단불참/지각/비매너) + **직접 입력** 중 선택 + 상세내용(선택) |
| 경고 삭제 | 경고현황(랭킹탭/퀵패널/회원관리 하단)에서 회원별로 펼쳐서 **개별 경고 항목 삭제** 가능 (오기록·소명 인정 시) |
| 경고 전체삭제 | 회원관리 목록 줄에서 바로 "경고 전체삭제" 버튼으로 해당 회원의 경고 기록 전부를 한 번에 삭제 가능 (되돌리기 불가, 확인창 필수) |
| 경고 표시 | 회원관리 목록에 "⚠️N" 뱃지, 3회 이상 누적 시 알림에 "강퇴 검토 대상" 표시 |
| 경고조치 현황 | 랭킹 탭 하단, 관리자 전용 — 회원별 누적 경고횟수 + 사유별 분류 표시 |
| 당일취소 연동 | 당일취소 발생 시 경고이력에도 "당일취소" 사유로 자동 등록되어, 4가지 사유(당일취소/무단불참/지각/비매너)가 하나의 경고조치 현황에 통합 표시됨 |
| 경고현황 퀵패널 | 알림 벨 왼쪽 "⚠️" 버튼(관리자 전용) — 랭킹 탭까지 안 가도 바로 경고 확인·삭제·페널티 부여/해제 가능 |
| 경고현황 (회원관리 하단) | 관리자 모드 진입 시 "👥 회원 관리" 바로 아래 "⚠️ 경고현황" 접이식 섹션 신설 — 동일한 경고 확인·삭제·페널티 부여/해제 가능 (총 3곳: 랭킹탭/퀵패널/회원관리 하단) |

## 23. 칭호 아이콘 (🎮 순위·실력과 무관한 재미 요소)

> **이 섹션 전체는 흥미유발용 재미 요소입니다.** 실력, 순위, 클럽 내 서열과 전혀 무관하며, 참석을 즐겁게 유도하기 위한 장치입니다.
> ⚠️ 참석 횟수 기반 "등급(브론즈~마스터)" 시스템은 위화감 우려로 **보류** — 운영진 회의 승인 시 재검토 예정 (설계는 보존됨)

| 항목 | 규칙 |
|---|---|
| 럭키 | 전체 방문 횟수가 **777/888/999/1000/1111**(이상)에 도달하면 축하 알림(전체공지) 발송. **관리자·운영진은 대상에서 제외**, **이미 어느 하나라도 당첨된 회원은 추가 당첨에서 배제**(한 사람당 평생 1회 한정). ⚠️ 이름 옆 아이콘·금빛 테두리 표시는 너무 화려하다는 의견으로 삭제됨(2026-07-20) — 당첨 자체와 축하 알림만 유지 |
| 최초 달성 | 사이트 내 최초로 100/200/300...회 달성 시 ⭐(개수는 100단위 수만큼) |
| 월간랭킹 메달 | 매달 랭킹 발표 시 1/2/3위에게 🥇/🥈/🥉 (최신 달 기준으로 갱신) |
| 칭호 선택 | 특별 칭호를 2개 이상 보유 시, 우측 상단 "🏅 칭호 선택"에서 표시할 하나를 직접 선택 가능 |
| 안내 | 가이드 메뉴에 "재미 요소"임을 설명 (1회성 안내 팝업은 반복 노출 문제로 삭제됨)

## 데이터 필드 참조표

> **알림 시스템 (2026-07-16 재설계)**: 로컬 캐시를 계속 동기화하는 방식을 버리고, 배지는 20초마다 가벼운 미확인 건수 조회, 패널은 열려있는 동안 5초마다 서버에서 통째로 재조회(병합 없음)하는 방식으로 단순화. 패널 안에 자체 새로고침 버튼도 있음.

> **저장 병합 방식 (2026-07-18 개선)**: 기존엔 "로컬에 그 키가 있으면 무조건 로컬 우선"이라, 오래된 스냅샷을 들고 있던 세션이 아무 저장이나 하면 다른 사람이 방금 바꾼 값(예: 3번코트 확보)을 되돌려버리는 문제가 있었음. 이제 `markDirty(field, key)`로 "이번 세션이 실제로 바꾼 키"만 추적해서, 그 키만 로컬 우선 적용하고 나머지는 항상 서버 최신값을 채택하도록 수정.

| 필드명 | 타입 | 삭제 시 표기 | 병합 보호 |
|---|---|---|---|
| `memberPins` | 객체 (name→PIN) | `null` | ✅ |
| `fingerprintChecks` | 객체 (name→{checked,ts}) | (삭제 안 함, false로 토글) | ✅ |
| `court3Confirmed` | 객체 (eventId→bool) | (삭제 안 함) | ✅ |
| `genderFlex` | 객체 (eventId→string) | (삭제 안 함) | ✅ |
| `preClosed` | 객체 (eventId→bool) | (삭제 안 함) | ✅ |
| `inviteCourts` | 객체 (eventId→number) | `null` | ✅ |
| `capacityOverrides` | 객체 (eventId→number) | `null` | ✅ |
| `cancelStats` | 객체 (name→number) | (삭제 안 함) | ✅ |
| `notifiedFlags` | 객체 (key→bool) | (삭제 안 함) | ✅ |
| `skippedDates` | 객체 | (삭제 안 함) | ✅ |
| `extraMembers` | 배열 (객체, name 기준) | 배열에서 제거 | ✅ (이름 기준 병합) |
| `removedMembers` | 배열 (문자열) | 배열에서 제거 | ✅ (합집합 병합) |
| `fingerprintExtraTargets` | 배열 (문자열) | 배열에서 제거 | ✅ (합집합 병합) |
| `templates` | 배열 (객체) | 배열에서 제거 | ⚠️ 미보호 (드물게 편집되어 우선순위 낮음) |
| `votes` / `comments` / `notifications` | — | — | 별도 테이블로 완전 분리, 해당 없음 |

## 24. 글자 크기 확대 (접근성 — 시력이 안 좋은 회원 배려)

| 항목 | 규칙 |
|---|---|
| 위치 | 상단 바 "🔤 글자크기" 버튼 |
| 단계 | 기본 → 크게(115%) → 더 크게(130%) → 기본... 순환 |
| 방식 (개선) | 처음엔 `zoom` 방식으로 만들었으나 아이폰 사파리에서 하단 네비·일부 패널에 안 먹는 문제 발견 → **모든 font-size 선언을 `calc(Npx * var(--font-scale))` 방식으로 전면 교체**, 버튼 클릭 시 JS로 `--font-scale` 값만 바꿔주는 방식으로 확실하게 전체 적용 |
| 안드로이드/삼성 대응 | 갤럭시 환경에서 확대가 안 먹는다는 피드백 확인 후, 안드로이드 브라우저 자체의 "글자 자동확대" 기능이 우리 계산식과 충돌할 가능성이 있어 `text-size-adjust:100%`를 전역 추가해 브라우저 자동조정을 무력화, `setProperty` 호출도 명시적 문자열로 전달하도록 보강 |
| 저장 | 브라우저별 로컬 저장, 다음 접속 시 자동 적용 |
| 상단 바 정리 | 글자가 커져도 버튼이 줄바꿈되며 지저분해지지 않도록 새로고침은 아이콘만, "비밀번호 변경"→"비번변경"으로 축약, 버튼 줄바꿈 스타일 보강 |

## 25. 복식 대진표 생성기 (메인 사이트에 완전 내장)

| 항목 | 규칙 |
|---|---|
| 배포 방식 | 닷홈 업로드 폴더가 이미지 파일만 허용해 iframe+별도파일 방식 포기, **메인 사이트 코드(헤더/바디/푸터)에 직접 통합** — 별도 파일 업로드 불필요 |
| 접근 경로 | 하단 네비 "🎾 대진표생성기" 버튼 → 화면 전체너비 패널 (기존 오늘현황 안 버튼에서 이동) |
| 충돌 방지 | CSS는 전부 `#bracketPanel` 스코프 안에서만 적용, JS는 IIFE로 감싸서 메인 앱의 전역변수(state, MEMBERS 등)와 충돌 없음 — 검증 완료 |
| 모드 | ① 개인 로테이션(매 라운드 파트너 중복 최소화, KDK 스타일) ② 팀 고정 대진표(2인1팀 고정 후 라운드로빈 리그전) ③ 단체전-팀vs팀(아래 상세) |
| 참석자 선택 목록 | 개인 로테이션·팀 고정 대진표 모드에서 여자/남자 섹션으로 분리, 각각 가나다순 정렬 |
| 회원 명단 | 도구 안에 현재 회원 63명이 미리 들어있어 체크만 하면 됨, 명단에 없는 이름은 직접 추가 가능 |
| 게임 수 카운트 | 대진표 생성 후 1인당(또는 팀당) 총 게임 수를 막대그래프로 표시 |
| 용량 영향 | 있음(전체 통합으로 파일이 커짐) — 닷홈 자체는 용량 제한 없음을 확인했으므로 문제되지 않음 |

### 25-1. 단체전(팀 vs 팀) 모드 상세

| 항목 | 규칙 |
|---|---|
| 용도 | A/B/C/D팀처럼 팀 단위로 구성(선수는 A-1~A-N 형태의 **일반 라벨**로 표시, 실명은 현장에서 대입) |
| 팀당 인원 | 제한 완화 — 최대 20명까지 구성 가능 (기존 6명 제한 삭제) |
| 최대 가능 인원 안내 | 코트 수 조정 시 실시간으로 "게임당 25분·4시간(9라운드) 기준, 코트 N개면 개인당 최소 3게임 보장 최대 약 (코트수×12)명" 안내 표시 (아래 정밀 표 참고) |
| 팀 수별 정밀 최대인원 (4시간·9라운드 기준) | 2팀: 3코트 36명/4코트 48명 · 3팀: 3코트 36명/4코트 48명 · 4팀: 3코트 **32명**/4코트 48명 — 팀 수가 많을수록 라운드 올림 계산 때문에 약간 더 빡빡해질 수 있음(특히 3코트+4팀 조합) |
| 팀 간 대진 | 라운드로빈으로 전체 팀이 서로 한 번씩 맞붙음(기존 팀고정 대진표와 같은 알고리즘 재사용) |
| 팀 내 개인전 | 각 팀-매치(tie) 안에서 파트너를 계속 바꿔가며 여러 게임 생성 (개인 로테이션과 같은 원리) |
| 게임 수 목표 | **개인당 최소 3게임** 보장을 목표로 tie당 게임수 자동계산: `ceil(팀인원×3÷2 ÷ 상대팀수)` — 팀 내 특정 개인이 아니라 **대회 전체 누적 기준**으로 공정하게 배분 |
| 일요일 프리셋 | "☀️ 3코트(9라운드)" / "☀️ 4코트 확보시(9라운드)" 버튼 — 게임당 25분·4시간(초반연습 5분 제외) 기준 |
| 안전장치 | 논리 라운드(팀 매치업) 경계를 넘어 게임이 섞이지 않도록 물리 라운드에 배정 — 같은 선수가 동시간대 두 코트에 겹치지 않음, 팀4~5개·다양한 코트수 조합 시뮬레이션 검증 완료 |
| 코트 활용도 | 팀 수가 적어 동시 매치업이 코트 수보다 적을 때(예: 4팀=2매치업, 3~4코트), 같은 매치업의 다른 게임을 선수 안 겹치는 선에서 남는 코트에 병렬 배정 — 코트를 최대한 활용하고 전체 소요 라운드도 단축됨 |
| 부전승/유연배치 | 홀수 인원이면 자동으로 로테이션에서 쉬는 순서가 공평하게 배정됨. 늦참/불참 시 팀 인원수만 조정 후 "다시 섞기"로 즉시 재생성 가능 |
| 용량 초과 안내 | 설정한 라운드보다 실제 필요한 라운드가 많으면 경고 문구 표시 |

## 26. 신규 회원 승인제

| 항목 | 규칙 |
|---|---|
| 등록 경로 ① 관리자 등록 | 관리자가 "신규회원추가"로 이름/성별/관내외를 입력하면 명단에는 즉시 반영되지만, **기본적으로 "승인대기" 상태**로 등록됨 |
| 등록 경로 ② 회원 자가등록 | 로그인 화면에서 "직접 입력..."으로 명단에 없는 이름을 넣으면(이름/성별/관내외 입력), **즉시 로그인되지 않고** 승인대기 상태로 등록만 되며 "가입 신청이 접수됐어요" 안내 후 로그인 중단됨 — 예전엔 이 경로로 바로 로그인이 돼버리던 허점이 있었음 |
| 중복 신청 처리 | 이미 승인대기 중인 이름으로 다시 시도하면 "이미 신청 접수, 승인 대기 중" 안내, 이미 정식 등록된 이름이면 "목록에서 선택해 주세요" 안내 |
| 로그인 차단 | 승인대기 상태인 회원이 로그인을 시도하면 "관리자 승인 대기 중" 안내와 함께 로그인이 막힘 (PIN 설정 여부와 무관하게 차단) |
| 표시 | 회원관리 목록에 "⏳승인대기" 표시 + "✅승인" 버튼이 뜸 |
| 승인 | 관리자가 "✅승인" 버튼을 누르면 즉시 승인대기 해제, 이후 정상적으로 초기비밀번호(0001)로 로그인 가능 |

## 27. 지문체크 패널 구조

| 항목 | 규칙 |
|---|---|
| 순서 | 제목 → **체크 버튼(최상단)** → 완료현황 → 검색/명단 순으로 배치, 안내설명 없이 바로 체크 가능 |
| 안내설명 위치 | 긴 설명("지문등록 갱신 및 출석 체크 안내")은 패널에서 빼고 가이드 안내 메뉴로 이동 |

## 28. 가이드 안내 구성

| 항목 | 규칙 |
|---|---|
| 순서 | "📖 이용 안내" → "📐 정원 알고리즘 강의노트" → "👋 신규회원 가이드(요약)" 순 |
| 신규회원 가이드 | 사이트엔 핵심만 요약, 상세 내용(가입절차·회비·에티켓 전문)은 네이버 밴드 공지사항 참조로 안내 |
| 상세 문서 | `이그니스_밴드공지_이용안내_신규가이드.md` — 밴드 업로드용 전체 상세 내용 별도 제공 |
| 칭호 항목 | 가이드에서 완전 삭제됨 |

## 29. 로고 & 파비콘

| 항목 | 규칙 |
|---|---|
| 현재 상태 | **새 로고 유지 중** — 코드 용량 문제로 원복을 고려했으나 보류, 나중에 코드가 너무 길어져 자리가 부족해지면 그때 다시 검토 |
| 헤더 아이콘 | "HANAM IGNIS TENNIS CLUB" 원형 로고, 52px 표시 (192px 고화질 소스) |
| 파비콘 | 동일 로고로 적용 중 |

## 30. 기타 UI 규칙

| 항목 | 규칙 |
|---|---|
| 다크모드 톤 | 밝은 원색 계열에서 채도를 낮춘 무채색에 가까운 톤으로 전면 조정 |
| 지문체크 제목 | "종합운동장 관내자 지문 출석체크 현황"으로 명확화 |
| 업데이트 히스토리 페이지당 표시 | 5개 → 10개 |
| 하단 네비 구성 | 오늘현황 / 업데이트히스토리 / 지문체크 / **🎾대진표생성기** / 가이드안내 (랭킹은 오늘현황 패널 안 "🏆 참석 랭킹 보기" 버튼으로 이동) |

## 31. templates(반복일정) 병합 보호

| 항목 | 규칙 |
|---|---|
| 병합 방식 | id 기준 병합 — 서버 최신본을 기본으로, 이번 세션이 실제로 추가·수정한 id만 로컬값 반영 (extraMembers와 같은 패턴) |
| 삭제 처리 | `removedTemplateIds` 배열로 별도 관리, 양쪽 세션의 삭제목록을 합집합으로 병합해서 삭제된 일정이 되살아나지 않게 함 |

## 32. 결산·통계 리포트 체계

| 항목 | 규칙 |
|---|---|
| 일간 결산보고 | 매일 낮 12:04 기준, 오늘 일정별 참석/대기/불참 현황 |
| 주간 결산보고 (기존) | 매주 일요일 22시, 참석 랭킹·복식무산취소·당일취소·지문체크 미완료 등 종합 (가장 상세한 리포트) — ⚠️ 2026-07-21에 이와 중복되는 별도 주간 리포트를 만들었다가 발견하고 제거함, 앞으로 주간 통계는 이 기존 리포트만 사용 |
| 월간 리포트 (신규) | 매월 1일 오전 9시, 지난달 요약(진행 일정수, 총 참석 연인원, 활동회원 수, 참석 top5, 신규 경고 건수) |
| 중복방지 | 전부 동일한 패턴(플래그+서버 재확인) |
| 월간 챔피언과의 관계 | 기존 monthlyChampions(랭킹 메달) 시스템과는 별개로 순수 통계만 제공 |

## 33. 관리자용 운영 대시보드

| 항목 | 규칙 |
|---|---|
| 위치 | 회원관리 옆 "📊 운영 대시보드" (관리자모드에서만 표시) |
| 내용 | 최근 4주 참석 추이(막대그래프, 진행중인 이번주는 제외) · 현재 페널티 적용중 회원 목록 · 최근 경고 이력(최신 15건) |
| 회비 | 총무가 별도 관리 중이라 포함하지 않음 |

## 34. 당일취소 예외 사유 & AI관리자 댓글 중복방지

| 항목 | 규칙 |
|---|---|
| 당일취소 예외 사유 | 시작 3시간 이내 취소 또는 복식무산 유발 취소 시, 사유를 선택하게 함: ① 우천 ② 부상위험 ③ 성별 인원 배려(복식 인원 맞추기 위해 양보) ④ 해당없음 — ①~③은 경고·페널티 집계에서 제외, ④(또는 프롬프트 취소)만 기존처럼 경고 집계 |
| 소명 이중체크 | ①~③으로 소명해서 경고가 면제되어도, 관리자에게는 "확인용" 알림이 별도로 뜸(자체소명이 실제와 다를 수 있어 확인 가능) |
| AI관리자 댓글 중복방지 | 등록 전 서버에서 최근 60초 내 같은 이벤트에 동일 내용의 AI관리자 댓글이 있는지 확인, 있으면 건너뜀 (여러 세션 동시접속으로 인한 중복 댓글 방지) |

## 35. AI관리자 멘트 다양화 & 신규 상황 자동화

| 항목 | 규칙 |
|---|---|
| 첫 참석 축하 | 생애 첫 참석시 환영 팝업+알림 (문구 3종 랜덤) |
| 럭키방문 임박 안내 | 마일스톤(777/888 등) 5명 이내로 근접하면 1회 안내 |
| 대기 5명 이상 제안 | 코트 추가/시간조정 검토 제안 |
| 관외비율 판단근거 | "총 참석 N명 기준 관외 최대 M명" 식으로 구체적 사유 명시 |
| 홀수인원 대진표 안내 | 4명씩 안 나눠떨어지면 시작 3시간 전부터 대진표생성기 활용 안내 |
| 문구 다양화 | 정원마감/첫참석 등 반복 빈도 높은 멘트는 3종 중 랜덤 |
| 결산보고 미승급자 요약 | 대기하다 끝난 인원 명단 추가 |
| 오늘현황 신규가입자 축하 | 최근 14일 내 승인된 회원 환영 문구 표시 |

## 36. 관내외 수정 · 3번코트 당일 리마인드 · 전체취소 · 처리이력 조회

| 항목 | 규칙 |
|---|---|
| 관내외 수정 | 회원관리에서 토글 버튼으로 관내↔관외 직접 수정 가능(이사 등 대응), `districtOverrides`로 저장 |
| 3번코트 당일 리마인드 | 유니온 오후/저녁 일정에 3번코트 필요한 날, 당일 새벽테니스 참석자에게 확보 부탁 알림+AI관리자 댓글 |
| 코트취소 (구 "전체취소") | 우천·공사·코트상황 버튼 → 사유선택 → 확정시 해당 일정 잠금 + 기존 참석·대기 전원 불참 자동전환(경고 대상 아님) — **saveVoteRow 호출 필수** (안 하면 화면엔 반영돼도 서버에 실제 저장 안 되는 버그 있었음, 2026-07-22 수정) |
| 코트취소 복구 | 취소 시점의 참석/대기 상태를 백업해두고, "✅ 코트취소 복구" 버튼으로 언제든 이전 상태로 되돌릴 수 있음 (비 그침 등 상황 개선시) |

## 39. 관리자 행동 되돌리기

| 항목 | 규칙 |
|---|---|
| 범위 | 댓글삭제, 경고부여, 페널티부여, 회원탈퇴처리 — 세션 로컬 저장(서버 미저장), 최대 10개 |
| 위치 | 관리자모드에서 상단에 "↩️되돌리기: [작업명] (최근 N개 기록됨)" 버튼 표시, 클릭시 확인 후 되돌림 |
| AI관리자 댓글 중복방지 기간 | 60초 → 30분으로 확대 (여러 세션 동시접속시 몇 분 간격 중복 방지, 2026-07-22) |
| 처리이력 조회 | 참석/대기 목록의 시간 표시를 클릭하면 최초 등록부터 승급·관리자이동까지 전체 이력 표시 (이력 데이터가 없는 옛날 등록건은 최소한 현재 상태 시각이라도 표시) |

## 37. 금요일 동시일정 안내 & 칭호아이콘 위치 & 마일스톤 확장

| 항목 | 규칙 |
|---|---|
| 금요일 20-22시 안내 | 유니온·종합운동장 동시 일정에 **참석/대기를 누른 시점**에 바로 "같은 성별끼리 모여서, 유니온 코트 우선 이용" 팝업 안내 (유니온 코트 이용기록 유지 목적), 댓글은 하루 1회만 |
| 3번코트 확보 댓글 | "정원이 N명→M명으로 늘어서 현재 K명 추가 참석 가능" 처럼 구체적 수치 명시 |
| 칭호 아이콘 위치 | 참석목록·랭킹에서는 표시 안 함, **AI관리자 댓글에서만** 표시 |
| 럭키방문 마일스톤 | 777/888/999/1000/1111/2000/2222/3000/3333/4000/4444 총 11종 |

## 38. 3분할 배포 안전 규칙 (중요, 향후 코드작업시 반드시 준수)

| 항목 | 규칙 |
|---|---|
| 문제 | 헤더/바디/푸터 3분할 배포시, 스크립트가 두 조각으로 잘리는데 ".onclick = 함수명;"처럼 함수를 **직접 참조**하는 코드가 그 함수의 실제 정의보다 먼저 실행되는 조각에 걸리면 "함수명 is not defined" 에러 발생 |
| 실제 사례 | closePanels 등 13곳에서 발생 확인 후 전부 수정 (2026-07-21) |
| 안전 규칙 | 앞으로 `.onclick = 함수명;` 형태로 직접 할당하지 말고, 반드시 `.onclick = () => 함수명();` 처럼 화살표함수로 감쌀 것 — 분할 지점이 어디든 클릭 시점(모든 스크립트 로드 후)에 실행되어 안전함 |
| 서비스워커 | PWA 삭제 후에도 `navigator.serviceWorker.register(...)` 코드가 남아있으면 콘솔에러 발생하니, PWA 관련 기능 삭제시 이 부분도 함께 확인 |

## 41. 테니스공 불출내역

| 항목 | 규칙 |
|---|---|
| 위치 | 하단 네비 "지문체크" 바로 옆에 "공불출내역" 메뉴 |
| 원탭 체크 (2026-07-25 추가) | 각 이벤트 카드의 참석목록 아래 "🎾 클럽볼 사용 체크" 버튼 — 이름·날짜·장소 자동 채움, 개수만 입력하면 바로 기록+재고차감+AI관리자 댓글. 개인볼은 이 버튼으로 불가, 공불출내역 패널에서 직접 지정 |
| 코트시간 기반 예상 사용량 | 1코트당 2시간에 클럽볼 1묶음(2개) 사용 기준(코트수×시간×0.5) — 종합운동장/유니온 각각 계산, **새벽테니스는 제외**. 일간·주간·월간 보고서에 실제 기록치와 함께 표시 |
| 묶음(세트) 단위 기록 (2026-07-25) | "개수" 직접입력 대신 "묶음 수 × 묶음타입(2개들이/4개들이)"으로 기록 — 실제 공 개봉 단위와 일치. `bundle_count`/`bundle_size` 컬럼에 저장, `count`는 자동계산(묶음수×묶음타입) |
| **단순화 (2026-07-27)** | "분실인가요?" 질문 완전 삭제 — 사용은 사용인 거라 세분화 안 함. 묶음수도 직접타이핑 대신 1/2/3/4/기타 객관식 버튼(`showChoiceDialog` 재사용). `reason` 필드는 항상 '사용'으로 고정 저장(과거 분실 데이터는 남아있으나 신규는 미구분) |

## 48. 참석 버튼 순서 & 불참사유 객관식 (2026-07-27)

| 항목 | 규칙 |
|---|---|
| 버튼 순서 | 참석 → 늦참 → 대기 → 불참 |
| 불참사유 | 확정("확정" 버튼) 시점에 "늦게 퇴근/레슨/개인사정/기타(직접입력)" 객관식 버튼으로 선택, 자동으로 "📌 불참 사유: ..." 댓글 등록 |
| showChoiceDialog | 재사용 가능한 객관식 버튼 다이얼로그 헬퍼 — 배경 오버레이+버튼목록, 배경 클릭시 취소(null 반환) |
| 분실 추적 | 기록시 "정상 사용" / "분실로 인한 추가개봉" 사유 선택 — 목록에 🚨분실 표시, 보고서에 기간별 분실건수·개수 통계 포함 |
| 재고 충전일 기록 | 재고 "직접입력"시 수량과 함께 충전 날짜도 입력 → `ballStockLastRefill`(위치별)에 저장, 패널에 "충전 M/D"로 표시 |
| 기록 항목 | 이름·날짜·장소(종합운동장/유니온)·볼종류(클럽볼/개인볼)·개수 |
| 표시 형식 | 예: "서힘찬 7/17 종합운동장 클럽볼 1", "임유나 7/16 유니온 개인볼 1" |
| 권한 | 누구나 기록 추가 가능, 개별 삭제·전체 초기화는 관리자만 |
| 데이터 | **(2026-07-25 정규화)** `ignis_ball_checkouts` 정식 테이블에 직접 저장/조회/삭제 (더 이상 state.data blob에 안 담김) |
| 클럽볼 재고 | `ballStock`(위치별 맵: 종합운동장/유니온 각각, MERGE_MAP_FIELDS로 병합) — 패널 상단에 위치별 + 합계 표시, 관리자가 위치별로 +/-/직접입력 조정, 해당 위치 클럽볼 불출시 그 위치 재고만 자동 차감 |

## 42. 데이터 정규화 — 경고·페널티·공불출내역 (2026-07-25)

| 항목 | 규칙 |
|---|---|
| 변경 내용 | warnings·penalties·ballCheckouts를 state.data JSON blob에서 분리해서 `ignis_warnings`/`ignis_penalties`/`ignis_ball_checkouts` 정식 테이블로 이전 |
| 페널티 이력 방식 변경 | 기존 "1인당 1건 덮어쓰기" → "이력 전체 보존(append-only)". 이름별 가장 최근 부여건이 자동으로 "현재 페널티"로 취급됨 |
| 캐시 패턴 | `state.data.warnings`/`state.data.penalties`는 이제 순수 로컬 캐시 — `refreshWarningDataFromServer()`가 테이블에서 조회해서 채움. 쓰기는 전부 테이블에 직접 |
| **배포 순서 (필수)** | ① `supabase_normalize_warnings_penalties_balls.sql`을 Supabase에서 먼저 실행 → ② 그 다음 이 코드를 배포. 순서가 바뀌면 경고/페널티/공불출 기능이 작동 안 함 |
| 중요 수정사항 | 일반 회원(비관리자)은 기존엔 초기 로드시 페널티 캐시가 안 채워져서 페널티 체크가 조용히 멈출 뻔한 결함 발견, init()에서 전체 회원 대상으로 캐시 로드하도록 수정 |

## 43. AI관리자 댓글 중복 근본수정 & 코트배치 제안 타이밍 (2026-07-27)

| 항목 | 규칙 |
|---|---|
| 중복방지 버그 | 마무리 인사말(즐테하세요/화이팅 등)이 랜덤이라, 기존 "완전일치(===)" 중복검사가 실제 중복을 못 잡던 문제 — 앞 30자(핵심내용)만 비교하도록 수정 |
| 코트배치 제안 타이밍 | 5분 전 → **1시간 전**으로 변경. 서버 재확인 로직 추가해서 여러 세션 동시감지로 인한 레이스컨디션 방지 |
| 긴급참석 승인 후 추가안내 | 1시간전 코트배치 제안이 이미 나간 뒤에 긴급참석 요청이 승인되면, 바뀐 인원 기준으로 "🚨 긴급 참석 승인으로 인원이 바뀌었어요" 댓글 추가 발송 |

## 44. 늦참(늦게 참석) 제도

| 항목 | 규칙 |
|---|---|
| 진입 위치 | 이벤트 카드 참석버튼 줄 — "참석" 바로 뒤 "⏰늦참" 버튼 (2026-07-27부터, 원래 게스트추가 옆이었으나 가시성 낮아서 이동) |
| 입력 흐름 | ① 늦는 시간 선택(5/10/15/20/25/30분 객관식 버튼 + 30분 이상은 직접입력, 2026-07-27부터 객관식으로 변경) → ② 늦참 사유 입력(댓글로 자동 등록) |
| 30분 이내 | **(2026-07-28 버그수정)** `setVote`를 재사용해서 일반 참석과 완전히 동일한 절차로 처리 — 정원 있으면 바로 참석, 정원 차있으면 일반 대기로 감(새치기 방지). 기존엔 정원체크 없이 무조건 강제 참석시켜서 먼저 대기 중이던 사람을 제치는 버그가 있었음 |
| 30분 초과 | "선대기" 상태(status:'waiting', type:'late') — 정원이 차있으면 자리 날 때까지 대기, 빈자리 생기면 자동승급 |
| 승급 우선순위 | **대기(regular) > 늦참(late) > 연참(consecutive)** — `waitingSortKey`에서 규칙 순위: regular=0, late=1, 그외=2 |
| 표시 | 참석목록에 "(늦참N분)" 태그 표시, 승급된 이후에도 태그 유지됨(4곳의 승급 로직 전부 lateArrival/lateMinutes 보존하도록 수정) |
| AI관리자 댓글 (2026-07-27 추가) | 등록시: "30분 이내라 바로 참석" 또는 "30분 초과라 선대기, 자리나면 자동승급" 안내. 승급시(4개 경로 전부): "늦참 선대기에서 참석으로 자동 승급되었습니다" 댓글 |

## 45. 공불출내역 — 미정산 개인볼 목록

| 항목 | 규칙 |
|---|---|
| 목적 | 개인볼 사용시 클럽이 나중에 갚아야 할 개수를 회원별로 추적 |
| 재고 영향 | **없음** — 실제로 클럽볼을 준 것만 재고에서 차감, 개인볼 사용은 재고와 무관 |
| 데이터 | `ignis_ball_checkouts`에 `settled`(boolean) 컬럼 추가 — 개인볼은 기본 false(미정산), 클럽볼은 true |
| 정산 처리 | 미정산 목록에서 관리자가 "정산완료" 버튼 클릭 → settled:true로 변경 |
| SQL 마이그레이션 | `supabase_add_ball_settled_column.sql` 실행 필요 |

## 46-1. 화·금 유니온 — 3번코트 확보 확률 안내 (2026-07-28, 2026-07-31/2026-08-02 수정)

| 항목 | 규칙 |
|---|---|
| 조건 | 오늘이 화요일 또는 금요일 + 유니온 일정 + **2코트 일정(court_count===2)** + 3번코트 아직 미확보 + **정원(8명) 이상 참석 + 대기자 존재** |
| 타이밍 | 특정 시각(예: 1시간전)에 묶여있지 않음 — 조건이 충족되는 즉시(다음 20초 주기 내) 발동. 2026-08-02에 "정원 8명" 조건을 명시적으로 추가해서 더 정확하게 함(이전엔 대기자 존재만 체크, 사실상 동일하지만 명시적으로 개선) |
| 버그수정 | 1코트 일정(예: 18-20 5번코트)에도 잘못 뜨던 문제 — court_count===2 조건 추가로 수정 |
| 근거 | 화/금은 새벽 레슨 인원이 많아 3번코트 확보 확률이 경험적으로 90% 이상 |
| 댓글 | 대기자가 있을 때만, 1회만 "확보 확률 90% 이상, 대기 상위 4명까지 추가 참석 가능할 것 같아요" 공개 댓글 |
| 개별 알림 | 대기 우선순위(`waitingSortKey`) 상위 4명에게 개별 알림 발송 |

## 46-2. 정원마감시 대기자 개별 안내 (2026-07-31)

| 항목 | 규칙 |
|---|---|
| 조건 | `checkCourtArrangementSuggestion`(1시간전 코트배치 제안) 발동 시점에 정원이 다 찼고(`regularTotal>=max_players`) 대기자가 있는 경우 |
| 예외 | 3번코트 확대 기대 가능한 상황(화/금+유니온+2코트+아직 3번코트 미확보)이면 이 안내는 생략 (희망이 있으니) |
| 내용 | 대기 중인 회원 각자에게 "정원이 가득 차서 지금은 참가가 어려울 것 같아요. 취소가 생기면 순서대로 자동 승급해드릴게요" 개별 알림 |

## 46-3. 시작 10분전 — 승급 어려운 대기자 자동 불참전환 (2026-07-31)

| 항목 | 규칙 |
|---|---|
| 조건 | 시작 10분 전, 정원마감(`regularTotal>=max_players`) + 대기자 존재 |
| 예외 | 3번코트 확대 기대 가능한 상황(화/금+유니온+2코트+아직 3번코트 미확보)이면 전환하지 않고 그대로 대기 유지 |
| 동작 | 대기자 전원을 실제로 `status:'absent'`로 전환(saveVoteRow), 전환된 회원 각자에게 개별 알림, 공개 댓글로 "🚫 정원이 차서 참석이 어려워, 대기 중이셨던 OOO, OOO님을 불참으로 전환했습니다" 등록 |
| 46-2와의 관계 | 46-2(1시간전 개별 사전안내)는 "예고", 46-3(10분전)은 "실제 전환"까지 진행 — 둘 다 유지됨 |

## 46. 지문체크 — 향후 2개월 종합운동장 일정

| 항목 | 규칙 |
|---|---|
| 배경 | 종합운동장 지문등록은 실제로 2개월마다 재등록이 필요 (기존 "2개월 주기 리셋" 개념과 동일) |
| 표시 위치 | 지문체크 패널 하단 |
| 표시 내용 | 오늘부터 2개월 이내 종합운동장 일정 개수 + 날짜 목록 (예: "8/15, 8/17 총 2개") |

## 47. 긴급 참석 요청 — AI관리자 댓글 & 7분 자동승인 프로토콜 (2026-07-27)

⚠️ **이 섹션의 "7분 자동승인" 부분은 회원에게 공개되는 "업데이트 히스토리"에는 반영하지 않음** (요청사항). 이 문서(RULES.md)는 내부 참고용이라 전체 기록.

| 항목 | 규칙 |
|---|---|
| 요청 시점 안내 (히스토리 반영됨) | 긴급 참석 요청 발생시 AI관리자가 "🚨 OOO님 N분전 참석요청 - 긴급승인 필요!" 댓글 자동 등록 |
| 7분 타임아웃 자동승인 (히스토리 미반영) | 관리자가 7분간 승인하지 않으면: **정원 미달**이면 승인 없이 자동으로 참석 승급 (관리자 부재시 긴급 프로토콜) / **정원이 꽉 찼으면** 거절(그대로 대기 유지, 별도 알림 없음) |
| 회원에게 보이는 화면 | 자동승인되어도 회원에게는 "✅ [일정] 긴급 참석 요청이 관리자 승인으로 처리되었습니다" 팝업이 뜸 (실제로는 타임아웃 자동처리였다는 사실은 노출 안 함) |
| 구현 방식 | `checkEmergencyRequestTimeout()`이 7분 경과+정원상태를 체크해서 처리, `autoApprovedPopupPending` 플래그를 세팅 → 해당 회원 세션이 `checkMyAutoApprovedPopup()`에서 자기 이름의 플래그를 발견하면 팝업 띄우고 플래그 제거 |

## 50. 연속참석 대기 — "시작 4시간 전" 제한 (2026-07-30 추가 → 같은 날 되돌림)

| 항목 | 내용 |
|---|---|
| 경위 | 2026-07-30 오전에 "연참은 시작 4시간 전부터만 승급" 규칙을 추가했으나, 운영진 회의에서 "코트 인원 확정 등을 고려하면 너무 빡빡하다"는 의견이 나와 같은 날 원래대로 되돌림 |
| 현재 규칙 | 연속참석 대기도 다른 타입과 동일하게 "당일 낮 12시(새벽테니스는 전날 23:50)" 기준으로 승급 — 시간 제한 없이 조건 충족시 즉시 승급 |
| 참고 | 혹시 나중에 이 "4시간 전" 아이디어가 다시 필요해지면, `tryAutoPromote`/`promoteWaitingForCapacity`/`promoteGenderFlexWaiting` 3곳의 waiting 필터에 `if(v.type === 'consecutive' && hoursUntilStart > N) return false;` 형태로 다시 추가하면 됨 (hoursUntilStart 변수 선언도 함께 필요) |

## 49. "성별대기(gender_wait)" 타입 승급 누락 버그 수정 (2026-07-30)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | 2코트 일정에서 한쪽 성별이 미확보시 상한(정원의 절반, 보통 4명)에 걸려 대기하면 `type:'gender_wait'`로 등록되는데, `promoteWaitingForCapacity`(3번코트 확보시 승급)·`forcePromoteWaitingNow`(관리자 즉시승급 버튼)·`tryAutoPromote`(일반 취소로 인한 승급) 3곳 전부 이 타입을 승급 대상에서 제외하고 있었음 |
| 실제 증상 | 정원이 8→12로 늘어나도 gender_wait 대기자는 영원히 승급 안 됨, 관리자가 수동으로 직접 참석추가해야 했음 |
| 수정 | 3개 함수 전부 `gender_wait` 제외 조건 삭제 — 기존에 있던 "성별당 6명 상한" 체크가 여전히 안전장치로 작동하므로, 이제 정원 확대/취소로 인한 빈자리 발생시 gender_wait도 정상적으로 우선순위대로 승급됨 |
| 검증 | 실제 시나리오(대기4명 중 성별대기 2명 포함, 정원 8→12) 시뮬레이션으로 재현 후 수정 확인 완료 |
| 영향받지 않는 곳 | `promoteGenderFlexWaiting`(성별유연승급 전용 함수)은 원래부터 gender_wait을 포함하고 있어서 문제 없었음 |

## 51. 럭키방문 마일스톤 중복당첨 버그 수정 (2026-07-30)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | `logVisit`/`checkLuckyVisitor`가 로컬 `state.data.milestoneRecords`만 보고 판단 — 세션이 오래 열려있으면(예: 몇시간) 그 사이 다른 사람이 서버에 당첨 기록을 남겨도 로컬은 그걸 모른 채 "미당첨"으로 계속 판단, 중복 당첨 발생 |
| 실제 사례 | 3333번째 방문 마일스톤에 이윤재(오후 4:07)와 최강영(오후 6:45) 두 명이 당첨 처리됨. 이후 저장 순서상 나중에 저장한 최강영으로 서버 값이 덮어써진 것으로 추정 |
| 수정 | `checkLuckyVisitor`에서 당첨 확정 직전 서버 `ignis_state`를 재조회해서 이미 기록된 사람이 있는지 재확인 — 있으면 로컬만 그 값으로 맞추고 조용히 종료(축하 알림 없음) |
| 남은 작업 | 이미 잘못 기록된 기존 데이터(예: 3333번째가 최강영으로 남아있는 것)는 코드 수정만으로는 안 고쳐짐 — 어느 쪽을 정식 당첨자로 할지 확인 후 `milestoneRecords` 값을 수동으로 정정 필요 (Supabase에서 `ignis_state` 테이블의 `value.milestoneRecords.lucky3333` 필드 직접 수정) |

## 52. 마일스톤 아이콘 관리 & 회원관리·오늘현황 표시 (2026-07-30)

| 항목 | 규칙 |
|---|---|
| 아이콘 커스터마이징 | `state.data.milestoneIcons`(key→아이콘) — 회원관리 "🎨 마일스톤 아이콘 관리"에서 20종 예시(🍀💰🔥🏆🎆🏅✨👑🌟💎🎇🎉🎊🥳⭐💫🚀🌈🎁💯) 중 선택 가능, `showChoiceDialog` 재사용 |
| 아이콘 조회 | `getMilestoneIcon(milestone)` — 커스텀 있으면 그것, 없으면 기본 아이콘(LUCKY_VISIT_MILESTONES.icon) |
| 회원관리 표시 | 회원 이름 옆에 당첨 마일스톤 아이콘 표시 (마우스오버/title에 몇번째인지) |
| 오늘현황 표시 | 패널 제일 아래에 전체 당첨자 리스트(아이콘+번째+이름) — 아직 아무도 없으면 섹션 자체 숨김 |
| 참고 | 기존 `getDisplayTitleIcon`(댓글용, 사이트최초/월간메달)과는 별개 시스템 — 럭키방문(lucky) 마일스톤 전용 |

## 53. 참석랭킹 위치 이동 & 결산보고 재배치 (2026-07-30)

| 항목 | 규칙 |
|---|---|
| 참석랭킹 | 오늘현황 패널 안에 있던 "🏆 참석 랭킹 보기" 버튼을 메인화면 최상단(위치탭 `#tabs` 바로 아래, `#topRankBtn`)으로 이동 — 오늘현황을 안 열어도 항상 보임 |
| 결산보고 재배치 | 오늘현황 내부 순서를 "신규회원환영 → 지문체크현황 → **오늘의 결산보고** → 일정목록 → 마일스톤당첨자" 순으로 변경 (기존엔 결산보고가 제일 마지막이었음, 참석랭킹 있던 자리로 당겨옴) |

## 54. 연속참석 "취소해도 자꾸 참석으로 바뀜" 버그 수정 (2026-08-01)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | A일정(예:20-22)에 참석 중이라 B일정(예:18-20)에 연속참석 대기(type:'consecutive')로 등록된 사람이, 나중에 A일정 참석을 취소해도 B일정의 연속참석 대기는 그대로 남아있어서, B일정에 자리가 나면 그대로 자동승급되어버림 — "취소했는데 자꾸 참석으로 바뀐다"는 문의의 원인 |
| 수정 | `cancelRelatedConsecutiveWaiting(ev, name)` 신설 — 누군가 참석(attend) 상태에서 다른 상태로 바뀔 때, 인접 일정에 연속참석 대기가 남아있으면 그것도 함께 취소(`saveVoteRow(..., null)`)하고 양쪽에 안내 |
| 호출 위치 | `setVote`에서 `status !== 'attend' && existing.status === 'attend'`(참석→다른 상태로 전환)일 때 호출 |
| 판정 기준 | `isConsecutiveCase(ev)`와 동일한 "인접 시간대(end_time↔start_time 일치)" 로직 재사용 |

## 55. "며칠 뒤 일정인데 당일취소 처리됨" 버그 수정 (2026-08-01)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | 당일취소 판정이 `isTimeBased(시작 3시간 이내) OR isDoublesBroken(복식 무산 유발)`로 되어있었는데, `isDoublesBroken`은 날짜 조건이 전혀 없었음 — 5일 뒤 일정도 복식이 깨지면 무조건 당일취소 취급됨 |
| 수정 | `isEventToday = ev.event_date === todayIso()`를 추가해서, 실제로 오늘 일정일 때만(`isEventToday && (isTimeBased \\|\\| isDoublesBroken)`) 당일취소 판정이 걸리도록 수정 |
| 결과 | 며칠 전에 미리 취소하는 경우, 복식이 깨지더라도 더 이상 당일취소 경고/사유선택 팝업이 뜨지 않음 |
| 후속 수정 (2026-08-01, 별도) | 위와는 별개 함수인 `checkDoublesBroken`(대체자 구하라는 알림+경고카운트)에도 날짜 체크가 빠져있어서 며칠 뒤 일정에도 계속 작동하던 것을 발견, `ev.event_date !== todayIso()`면 즉시 return하도록 수정 |

## 56. 운영 대시보드 — 최근 30일 회원별·시간대별 통계 (2026-08-01)

⚠️ 이 섹션은 회원공개용 "업데이트 히스토리"에는 중립적으로만 기록(코트독점 관련 언급 없음). 이 문서는 내부 참고용이라 상세 기록.

| 항목 | 규칙 |
|---|---|
| 위치 | 운영 대시보드 하단 "📈 최근 30일 회원별·시간대별 통계 보기" 버튼 (관리자 전용) |
| 데이터 소스 | `getEventsInDateRange(최근30일)` + `state.data.votes` — 실시간 앱 상태 기준 (별도 DB 직접조회 없음, Claude가 라이브 DB에 접근할 수 없어 이 방식으로 구현) |
| 회원별 통계 | 최근 30일 attend 횟수 내림차순 목록 |
| 시간대별 통계 | `start_time-end_time` 슬롯별 attend 총 횟수 |
| 코트독점(편중) 감지 | 평균(mean) 참석횟수의 **2배 이상 & 최소 8회 이상**인 회원을 🔺 표시 + 상단에 별도 안내 박스로 강조. 소수 데이터 노이즈 방지를 위해 "최소 8회" 조건 추가. 특정인을 지목하는 게 아니라 데이터 기반 참고용이라는 문구 포함 |
| 함수 | `renderMonthlyMemberStats()` — `renderDashboard()`의 새 버튼에서 호출 |

## 57. 신규 관내회원 — 지문체크 대상목록 자동연계 (2026-08-02)

| 항목 | 규칙 |
|---|---|
| 적용 시점 | `approveMember(name)` — 가입 승인이 완료되는 순간 (관리자 직접추가/본인 가입신청 두 경로 모두 결국 이 함수를 거침) |
| 조건 | 승인된 회원의 `district === '관내'`인 경우 |
| 동작 | `state.data.fingerprintExtraTargets`에 이름 추가 (이미 `getFingerprintTargets()`에 있으면 중복추가 안 함) |
| 참고 | `newMemberAddBtn`(관리자 직접추가) 핸들러에도 동일 로직이 한 번 더 있음 — 두 곳 다 있어도 중복 체크가 있어 안전함(둘 중 어느 경로로 들어와도 최종적으로 승인 시점에 한 번 더 확인됨) |

## 58. "탭이 안눌리거나 남의 정보가 뜨는" 버그 근본원인 수정 (2026-08-02)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | `pollForUpdates()`가 20초마다 서버와 동기화하는데, **다른 회원이 아무 일정에서나 투표를 바꾸면** `freshVotes !== state.data.votes` 조건이 참이 되어 `renderEventList()`(전체 목록 재구성)가 무조건 실행됨. 다회원 실시간 앱 특성상 이 재렌더링이 매우 자주(바쁜 시간대엔 거의 매 주기) 발생 |
| 증상 | 사용자가 화면(특히 이력조회용 `.chip-ts` 시간 텍스트)을 탭하는 정확한 순간에 재렌더링이 겹치면, 원래 DOM 요소가 사라지거나(탭 씹힘) 같은 화면 위치에 다른 사람의 새 요소가 들어서서(엉뚱한 사람 정보 표시) 발생 |
| 수정 | 전역 `userIsInteracting` 플래그 신설 — `touchstart`/`mousedown` 발생시 true로 설정, 0.5초 후 자동으로 false. `pollForUpdates()`의 두 재렌더링 트리거(state 전체 동기화, votes 동기화) 모두 `userIsInteracting`이면 이번 주기는 건너뛰고 다음 20초 주기에 자연스럽게 반영 |
| 데이터 정합성 | 건너뛴 경우 `lastDataSnapshot`도 갱신하지 않으므로, 다음 주기에 여전히 다르면(즉 아직 미반영이면) 정상적으로 다시 시도됨 — 데이터 유실 없음, 화면 반영만 살짝 늦춰짐 |

## 59. 3번코트 확보확인 팝업 — 참석회원 확장 (2026-08-02)

| 항목 | 규칙 |
|---|---|
| 기존 | `checkCourt3ConfirmPopup()`이 관리자(`hasAdminPower()`)에게만 발동 |
| 변경 | 관리자이거나, 해당 일정에 실제 참석중(`status==='attend'`)인 회원이면 발동 대상에 포함 |
| localStorage 키 | 기존엔 `court3-popup-asked-{eventId}`(전역 공유), 이제 `court3-popup-asked-{eventId}-{myName}`(사람별로 각자 하루 1회) — 여러 참석자가 각자 독립적으로 확인 기회를 가지도록 수정 |
| 확인시 | 관리자든 참석회원이든 동일하게 "확인" 누르면 `toggleCourt3Confirmed(ev.id)` 호출 → 정원 자동 확대 + 대기자 자동승급 |
| 배경 | 화/목 새벽, 일요일, 토요일 20-22 등 3번코트 관련 일정에서 관리자가 매번 직접 확인해야 했던 부담을 줄이기 위함 — 참석 중인 아무나 확인해줘도 되게 |

## 60. 관외 표시 색상 강화 + 관외 우선권 로직 (2026-08-04)

| 항목 | 규칙 |
|---|---|
| 색상 | `.chip-district-gwae`(관외 태그): `--accent-soft`/`--accent-dark`로 관내와 구분. `.ev-full-badge`(관외마감 등 배지): font-size 10→13px, font-weight 700→800, 배경을 `--danger-soft`(연한색)에서 `--danger`(진한 빨강) 실색+흰글씨로 변경 |
| 관외 우선권 로직 | `setVote`의 관내(비관외) attend 등록 경로에, 관외비율 처리 블록 직후 새 체크 추가: `ev.location==='종합운동장' && district!=='관외'`일 때, gwae_wait 대기자가 존재하고 `counts.gwaeAttend < gwaeLimit(counts.regularTotal, court_count)`(내가 들어가기 전 기준으로 이미 관외 한명 더 받을 여유가 있음)이면, 새 관내 신청자를 'waiting'(regular)으로 보내고 `tryPromoteGwaeWaiting(ev)` 즉시 호출 |
| 발생 조건 | 관외 대기자가 등록된 후 다른 관외가 취소해서 gwaeAttend가 줄었는데 아직 자동승급이 안 된 "틈"에 새 관내가 신청하는 경우 등 |
| 검증 | 시뮬레이션(총원3명·관외0명·관외한도1명 상황에서 새 관내 신청)으로 관외우선권 발동 조건 확인 완료 |

## 61. 연속참석 우선순위 — 성수기 시범규칙 (2026-08-04, 운영진 회의 결과)

⚠️ 회원 공개용 SITE_LOG에는 "운영진 회의 결과"로만 간단히 기록. 이 섹션은 내부 구현 상세.

| 항목 | 규칙 |
|---|---|
| 배경 | 연참 우선순위 자체는 이미 최하위(일반대기>늦참>연참)였지만, "낮 12시에 경쟁자가 없으면 그냥 승급되어버려서 당일 늦게 신청하는 회원이 자리를 못 잡는" 구조적 문제가 있었음 |
| 시즌 판별 | `isPeakSeason()` — `OFF_SEASON_MONTHS = [12,1,2]`(비수기), 그 외 전부 성수기. 상수 배열이라 나중에 조정 쉬움 |
| 자동승급 3곳 수정 | `tryAutoPromote`, `promoteWaitingForCapacity`, `promoteGenderFlexWaiting` — 필터에 `if(isPeakSeason() && v.type === 'consecutive') return false;` 추가. 성수기엔 연참이 이 3개 자동경로에서 아예 승급 후보가 안 됨 |
| 신설 함수 | `checkConsecutiveWaitingLatePromotion()` — `isPeakSeason()`이 아니면 즉시 return. 시작 1시간 전(`minutesUntil<=60 && >=0`), 딱 1회, 자리 있는 만큼(최대 20회 루프) 연참 대기자를 우선순위(`waitingSortKey`)대로 승급. 댓글+개별알림 발송 |
| 자리 없을 때 | 1시간전에도 자리가 없으면 그대로 대기 유지 → 기존 `checkWaitlistToAbsentConversion`(10분전)이 모든 대기타입에 동일하게 적용되므로, 자동으로 불참 전환됨 (별도 처리 불필요) |
| 관리자 예외 | `forcePromoteWaitingNow`(지금 자동승급 실행 버튼)는 이 제한 없음 — 명시적 관리자 판단이므로 |
| 겨울(비수기) | 기존 방식 그대로 — 낮 12시부터 일반대기와 동일 조건(우선순위만 낮음)으로 경쟁 |
| 검증 | 실제 함수 추출 후 시뮬레이션 2건 — ① tryAutoPromote: 일반대기 승급/연참 대기유지 확인, ② checkConsecutiveWaitingLatePromotion: 1시간전 연참 정상 승급 확인 |

## 62. 클럽볼 사용체크 — "묶음" 개념 제거 (2026-08-04)

| 항목 | 규칙 |
|---|---|
| 변경 전 | "몇 묶음(세트) 사용하셨나요?" — 1/2/3/4묶음 선택 후 내부적으로 ×2(묶음당 개수)해서 count 계산 |
| 변경 후 | "몇 개 사용하셨나요?" — 2/4/6/8개 버튼 + 기타(직접입력)로 실제 개수를 바로 입력. `bundle_count`/`bundle_size` 필드는 더 이상 신규 기록에 사용 안 함(과거 데이터에는 남아있어 목록 표시시 하위호환 처리됨) |
| 적용 범위 | `checkBallQuickUse`(이벤트카드 원탭), 공불출내역 패널 상세입력(`ballCountInput`) 둘 다 |
| 재고 직접입력 | 관리자용 재고 수량 직접입력(`data-ball-stock-set`)은 원래부터 0 이상 임의의 숫자로 자유롭게 수정 가능했음 — 별도 수정 불필요 |

## 63. 전체 기능 전수 재검증 (2026-08-04)

| 항목 | 내용 |
|---|---|
| 배경 | 세션 중 작업환경 초기화가 있었고, outputs에 남아있던 v100 조립본(preview)에서 복원해서 작업을 이어갔음. 이 복원본은 "복식무산체크 당일취소아니면..." 수정을 반영하기 **전** 시점의 것이었을 가능성이 있어 누락 발생 |
| 발견 | `checkDoublesBroken`에 `if(ev.event_date !== todayIso()) return;` 가드가 빠져있던 것을 발견, 재적용 |
| 재검증 방법 | v70~v100 사이 만들었던 주요 기능 26개+를 문자열 존재여부로 전수 체크(정규식 특수문자 오탐 1차 발생 후 순수 문자열 매칭으로 재검증), 구문검증·ID참조·중복함수·회원데이터 파싱·핵심 로직 시뮬레이션(관외한도·우선순위·코트시간공식) 전부 재실행 |
| 결론 | 이 1건 외 누락 없음 확인 |
| 교훈 | 앞으로 세션 중 작업환경 초기화가 감지되면(파일 없음 등), 복원 직후 **꼭 이런 전수 체크리스트로 재검증**할 것 — 특정 시점 조립본에서 복원하면 그 이후~직전 세션에서의 변경사항이 누락될 수 있음 |

## 64. 창고재고 / 관리자 대기유형 세분화 / 참석랭킹 마일스톤 (2026-08-04)

| 항목 | 규칙 |
|---|---|
| 창고재고 | `ballStock`에 `'창고'` 키 추가 — defaultData 및 5곳의 fallback 초기화 전부 일괄 반영. `stockRow('창고')` 재사용으로 +/-/직접입력 버튼 자동 지원(querySelectorAll 순회라 loc값 무관하게 동작). 총합계에도 창고 포함 |
| 창고 용도 제한 | 사용 기록(체크아웃)의 "위치" 선택지(종합운동장/유니온)에는 창고를 넣지 않음 — 창고는 "보관 장소"이지 "사용 장소"가 아니므로 재고 표시/조정 전용 |
| 관리자 대기유형 세분화 | 참석자 칩의 "이동▾" 드롭다운에서 기존 "대기로"(단일) → "대기로 (일반/관외대기/성별대기/연속참석/늦참)" 5종으로 분리. `adminMoveMember`에서 `action.startsWith('waiting-')`로 파싱해서 해당 type으로 vote 생성. 기존 `action==='waiting'`(구버전 호출) 케이스도 하위호환으로 유지 |
| 참석랭킹 마일스톤 리스트 | `renderRankBody()` 하단에 오늘현황과 동일한 럭키방문 마일스톤 당첨자 리스트 추가(같은 `LUCKY_VISIT_MILESTONES`/`getMilestoneIcon` 재사용) |

## 65. 관리자 참석 이동시 승급사유 지정 (2026-08-04)

| 항목 | 규칙 |
|---|---|
| 표시 라벨 확장 | `PROMOTED_LABELS` 맵 신설 — consecutive(🔁연참승급)·wait(⬆️대기승급)·gwae_wait(⬆️관외대기승급)·gender_wait(⬆️성별대기승급)·late(⬆️늦참승급)·regular(⬆️일반대기승급) |
| 관리자 참석이동 흐름 | `adminMoveMember`의 `action.startsWith('attend-')` 처리에서, 성별 파싱 후 `showChoiceDialog`로 "참석 처리 사유"(직접등록/일반대기승급/관외대기승급/성별대기승급/연속참석승급/늦참승급) 선택 → `promotedFrom`에 매핑 저장 |
| 적용 범위 | "이동▾" 드롭다운으로 직접 참석 이동시킬 때, "회원선택(참석추가)" 하단 드롭다운으로 신규 추가할 때 둘 다 이 다이얼로그를 거침(둘 다 내부적으로 attend- 액션 사용) |

## 66. 창고재고 (v102)

`ballStock`에 `'창고'` 키 추가 — defaultData 및 fallback 6곳 일괄 반영. `stockRow('창고')` 재사용으로 +/-/직접입력 지원. 사용 기록 위치 선택지에는 포함 안 함(보관용이지 사용장소 아님).

## 67. 마일스톤 기능 분리 (v102) — 상세는 "이그니스_마일스톤_기능_별도보관.md" 참고

럭키방문(777~4444)+사이트최초N회달성 시스템 전체를 사이트에서 제거, 별도 파일로 보관. 월간랭킹 메달·첫참석환영(`checkFirstAttendance`)은 분리되어 유지됨. `milestoneRecords`/`milestoneIcons`/`milestonesCelebrated` 필드도 defaultData/MERGE_MAP_FIELDS에서 제거. HTML 105줄 감소(v101 대비).

**재적용시 반드시 위 별도보관 파일의 체크리스트를 따라 복원할 것.**

## 68. 가이드 확장 & 오늘현황 최상단 배치 (v102, 2026-08-05)

| 항목 | 내용 |
|---|---|
| 가이드 확장 | "정원 알고리즘 강의노트"에 ⑤대기승급우선순위 ⑥늦참제도 ⑦연속참석(성수기시범규칙 포함) ⑧게스트참석 항목 신설, "클럽볼 이용안내" 섹션도 신설 |
| 오늘현황 최상단 | `toggleTodayPanel()` 함수로 로직 분리(재사용), `#topTodayBtn`을 `#topRankBtn`과 나란히 배치(flex). 하단 네비 `todayPanelToggle`은 그대로 유지, 상단은 "추가 접근경로"로 붙인 것 |

## 69. 회원별 상세 통계 확장 (v104, 2026-08-05)

| 항목 | 규칙 |
|---|---|
| 시간대 분류 | `classifyEventCategory(ev)` — 토요일/일요일은 시간 무관하게 요일로 분류, 평일은 시작시각 기준(10시 미만=새벽코트, 20시 미만=18-20시, 그 외=20-22시) |
| 기간 선택 | `renderMonthlyMemberStats(period)` — '30일'/'이번달'/'이번분기'/'올해' 4종, 버튼 클릭시 즉시 재계산 |
| 1일 2회 참석 | 회원별로 날짜별 attend 횟수를 집계해서 2회 이상인 날짜 수를 카운트 |
| 연속 출석일수 | `isNextDayIso`로 날짜 배열을 순회하며 연속 구간(스트릭) 길이를 판정, 2일/3일/4일+ 로 분류 카운트 (시뮬레이션 검증: 8/1-8/3 3일연속 + 8/5-8/6 2일연속 정확히 계산됨) |
| 대기미승급 | 해당 기간 votes에서 `status==='waiting'`으로 남아있는(즉 끝까지 승급 못한) 건수 카운트 |
| 강조 표시 | 20-22시(황금시간대) TOP5, 연속참석(1일2회+스트릭 합산) TOP5를 표 상단에 별도 박스로 강조 |
| 데이터 소스 | 기존과 동일하게 `getEventsInDateRange` + `state.data.votes` (실시간 앱 상태, 별도 DB 직접조회 없음) |

## 70. 관리자모드 패널 순서 재정비 (v104, 2026-08-05)

| 항목 | 내용 |
|---|---|
| 새 순서 | 운영대시보드 → 회원관리 → 반복일정목록 → 일정추가 |
| 경고현황 통합 | 별도 `warnStatusPanel`(details)을 제거하고, `renderDashboard()` 안에 `renderDisciplinaryHtml()`+`wireDisciplinaryButtons(box)` 추가 — 대시보드 하나로 통합 |
| 정리한 잔여참조 | `warnStatusPanel`/`warnStatusBody` 관련 — style.display 토글, `renderMemberMgmt()` 내 호출, 새로고침버튼 분기, `renderWarnStatusPanel()` 함수 자체까지 전부 제거(런타임 오류 방지) |

## 71. 참석랭킹 새벽코트·평일·주말 부문 분리 (v104, 2026-08-05)

| 항목 | 내용 |
|---|---|
| UI | 기존 기간 탭(주간/월간/분기) 아래에 카테고리 탭(전체/새벽코트/평일/주말) 신설, 두 그룹 독립적으로 토글(`.rank-tab[data-period]` / `.rank-tab[data-category]` 분리 셀렉터) |
| 카테고리 매핑 | `classifyEventCategory`(5분류: 새벽코트/18-20시/20-22시/토요일/일요일)를 3분류로 축소 — 새벽코트=새벽코트, 18-20시·20-22시=평일, 토요일·일요일=주말 |
| computeRanking 확장 | `category` 파라미터 추가, '전체'가 아니면 `getEventsInDateRange`+`classifyEventCategory`로 필터링 후 집계 (전체는 기존 `extractDateFromEventId` 방식 그대로 유지, 성능/하위호환) |
| 검증 | 화요일새벽→새벽코트, 화요일20-22→평일, 토요일20-22→주말, 일요일새벽→주말 4개 케이스 시뮬레이션 확인 |

## 72. 오늘현황 — 결산보고 실시간표시 제거, 알림 전용화 (v104, 2026-08-05)

`renderTodayPanel()`에서 `dailySummaryHtml`(실시간 결산보고 표시) 제거. `buildDailySummary()` 함수와 하루 1회(오후 12:04) 알림 발송 로직(`checkDailyNotifications` 관련)은 그대로 유지 — 이제 결산보고는 알림으로만 받아볼 수 있음.

## 73. 회원별 상세통계 최상단 패널 분리 + 늦참표시 개선 (v105, 2026-08-05)

| 항목 | 내용 |
|---|---|
| 최상단 패널 | `memberStatsPanel`(신규 details) — 관리자패널 순서 최상단, `dashboardPanel`보다 위. 관리자모드 진입시 자동 렌더링(`renderMonthlyMemberStats()` 즉시 호출) |
| 늦참표시 | `lateTag`를 이름 옆에서 시간(`chip-ts`) 뒤로 이동, 형식 `(늦N분)`로 단순화 |

## 74. "늦참+연속참석" 동시상황 우선순위 버그 수정 (v105, 2026-08-05)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | `checkLateArrival`의 30분초과 분기가 `isConsecutiveCase(ev)`를 전혀 체크하지 않고 무조건 `type:'late'`로 등록 — 연참 상황이어도 늦참(우선순위1)으로 취급되어, 순수 연참(우선순위2)보다 먼저 승급되던 문제 |
| 수정 | `checkLateArrival`에서 `isConsecutiveCase(ev)` 체크 추가, 참이면 `type:'late_consecutive'`로 등록 |
| 새 우선순위 | `waitingSortKey`에 4번째 티어 추가: 일반(0) > 늦참(1) > 연참(2) > **늦참+연참(3, 최하위)** |
| 승급 경로 | `late_consecutive`는 `tryAutoPromote`/`promoteWaitingForCapacity`/`promoteGenderFlexWaiting` 화이트리스트에 없어 항상 자동승급 제외. 유일한 승급 경로는 `checkConsecutiveWaitingLatePromotion`(시작 1시간전) — 이 함수를 계절 무관하게 항상 실행되도록 재설계(순수 연참은 성수기에만 이 경로 이용, late_consecutive는 계절 무관 항상 이 경로가 유일한 승급 수단이므로 영구 승급불가 방지 목적) |
| 연쇄 처리 | `waitConsecutive` 카운트, `cancelRelatedConsecutiveWaiting`(인접일정 취소시 연쇄취소) 둘 다 `late_consecutive` 포함하도록 확장 |
| 표시 라벨 | `PROMOTED_LABELS`에 `late_consecutive:'🔁늦참+연참승급'` 추가 |
| 검증 | 4가지 유형(일반/늦참/연참/늦참+연참) 혼재 시뮬레이션으로 정확한 승급순서 확인 완료 |

## 75. 자동승급 관련 시점 통일 (v105, 2026-08-05)

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `checkGenderFlexPromotions`(성별유연승급 판정) | 일정 전날 오전 11:50 (고정 시각) | **당일 낮 12시** |
| `checkCourt3ConfirmPopup`(3번코트 확인팝업) | 당일 오전 11시부터 | **당일 낮 12시부터** |
| `tryAutoPromote`(일반 자동승급) | 당일 낮 12시(새벽은 전날 23:50) | 변경 없음(기준점) |

**통일 안 한 것**: `checkConsecutiveWaitingLatePromotion`(시작 1시간전), `checkWaitlistToAbsentConversion`(시작 10분전)은 "시작까지 남은 시간"이 로직의 핵심이라 통일 대상에서 제외. `promoteWaitingForCapacity`/`tryPromoteGwaeWaiting`은 이벤트 트리거(특정시각 아님)라 해당없음.

**성별유연승급 관련 변경사항 검토**: `checkGenderFlexPromotions`의 판정 기준일이 "일정 전날"에서 "당일"로 바뀜에 따라, 루프 대상도 `ev.event_date === t`(당일만)로 단순화(기존엔 `t` 또는 `tmr` 이틀치 검사). **하루 전 미리 확정**이라는 기존 설계 취지가 약해지는 트레이드오프가 있음을 SITE_LOG에 명시적으로 남김 — 저녁일정(18-20/20-22시) 기준 사전예고 시간이 24시간→6~8시간으로 단축됨.

**안전성 확인**: court_count===2(성별유연승급 대상) 템플릿 전수 조사 결과 전부 18시 또는 20시 시작이라, "당일 낮 12시" 판정이 일정 시작보다 항상 앞섬(새벽테니스·일요일 오전 1부(10-12시, 3코트라 대상 아님) 등은 애초에 court_count≠2라 무관).

## 75-1. 성별유연승급 판정시점 — "전날 낮 12시"로 재조정 (v105, 2026-08-05 후속)

위 75번 통일 직후, "하루 전 미리 확정" 취지를 되살리기 위해 성별유연승급만 다시 조정됨.

| 항목 | 최종 상태 |
|---|---|
| `checkGenderFlexPromotions` | **일정 전날 낮 12시(정오) 고정** — `prevDay`(시작일-24시간) 날짜의 12:00:00으로 계산. 루프 대상도 다시 `t`(당일) 또는 `tmr`(익일) 이틀치 검사로 복원 |
| `checkCourt3ConfirmPopup` | 당일 낮 12시 (75번에서 변경한 상태 유지) |
| `tryAutoPromote` | 당일 낮 12시(새벽은 전날 23:50) (변경 없음, 기준점) |
| 계산 검증 | 시뮬레이션: 수요일 20:00 일정 → decisionTime = 화요일 12:00:00 정확히 계산됨 확인 |
| 관련 문구 | 가이드 2곳, AI관리자댓글 1곳, 대기유형 힌트 1곳 전부 "전날 낮 12시" 기준으로 재수정 |

**결론**: 세 메커니즘이 완전히 동일한 시각(당일 낮12시)으로 통일되진 않았지만, "숫자"(12시 정각)는 통일되었고 "기준일"만 성별유연승급이 하루 앞서 있는 형태로 최종 정리됨. 이 형태가 각 로직의 설계 취지(성별유연승급=사전예고용, 나머지=당일 처리용)에 더 부합함.

## 76. 종합운동장 — 관외 참석 "관내 최소 3명" 조건 신설 (v106, 2026-08-05)

| 항목 | 규칙 |
|---|---|
| 배경(기존 사각지대) | 기존 관내:관외 비율표는 "총 인원" 기준이라, 관내 0명+관외1명(본인)=총1명도 "4명이하→관외1명" 조건을 통과해 바로 참석되는 사각지대가 있었음 |
| 새 조건 | 관내 참석 인원이 **3명 미만**이면, 기존 비율 계산과 무관하게 관외는 무조건 대기(`gwae_wait`) — 관내 3명 이상부터 기존 비율 계산 적용 |
| 적용 위치 | ① 신규 등록시(`setVote`의 관외 등록 블록 최상단에 게이트 추가) ② `tryPromoteGwaeWaiting`(대기자 자동승급 로직)에도 동일 조건 추가 — 대기 중이던 관외도 관내 3명 미만이면 승급 보류 |
| 검증 | 관내 0/1/2/3/4명 각각 시뮬레이션, 경계값(정확히 3명)에서 차단→통과 전환 확인. 기존 버그 사례(관내0+관외1)도 새 게이트로 정상 차단됨 재확인 |
| 가이드 반영 | "④ 종합운동장 관내:관외 비율" 항목에 "관내 최소 3명 전까지 관외 참석 불가" 문구 추가 |

## 76-1. 교차검증 발견 — 관외우선권↔관내최소3명 충돌 수정 (v106, 2026-08-05)

| 항목 | 내용 |
|---|---|
| 발견 경위 | 76번 규칙 추가 직후 전체 교차검증 과정에서, 65번(관외우선권) 규칙과의 상호작용을 재확인하다 발견 |
| 버그 시나리오 | 관내 3명 미만 + 관외대기자 존재 상태에서 새 관내가 신청하면, 관외우선권 로직(`gwaeWaitList.length>0 && gwaeAttend<currentLimit`)이 발동해 새 관내를 대기로 보내고 `tryPromoteGwaeWaiting`을 호출하는데, 이 함수는 `naeAttend<3`이면 즉시 break — 결과적으로 새 관내도 대기, 관외도 승급 안 되는 교착 상태 발생 |
| 수정 | 관외우선권 발동 조건에 `counts.naeAttend >= 3` 추가 — 관내 3명 미만이면 이 로직 자체를 건너뛰고 새 관내는 정상 참석 |
| 검증 | 관내2명(발동안함, 정상참석) / 관내3명(정상발동, 대기+우선권이전) 두 시나리오 시뮬레이션으로 수정 전/후 비교 확인 |

## 77. 대기유형 표시/이력 오류 3건 수정 (v107, 2026-08-05)

| 항목 | 내용 |
|---|---|
| 늦참대기 미분류 버그 | `computedCounts`의 waiting 타입 분기에 `type==='late'` 케이스가 없어서 최종 else절(`waitRegular`)로 떨어져 일반대기로 잘못 카운트되던 버그. `waitLate`/`waitLateNames` 신설, 리턴 객체·`waiting` 총합·오늘현황 waitAll·`renderMyWaitingNotice`의 typeMap까지 전부 반영(`late_consecutive`도 typeMap에 추가) |
| 오늘현황 대기 누락 | `waitAll` 조합에 `waitGwaeWaitNames`(관외대기)가 빠져있던 것을 포함, 관외대기자 있으면 긴급안내 문구("관내 회원이 더 오시면 바로 참석 가능해요") 추가 표시 |
| 이력표시 오해 | `showVoteHistory`에서 `h.actor`(실제 처리를 실행한 사람, 관리자 대리처리 등)를 그냥 이름만 나열해서 "다른 사람 이력이 섞인 것"처럼 보이던 문제. `actor !== name`일 때만 "(관리자 OOO님이 처리)"로 명확히 표시하도록 수정 |

## 78. 관외+연속참석 동시상황 — 관외 비율규칙 우회 승급 버그 수정 (v107, 2026-08-05)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | `setVote`의 분기 순서상 `isConsecutiveCase(ev)` 체크가 관외 규칙(관내최소3명+비율) 체크보다 먼저 실행됨 — 관외 회원이 연참 상황이면 관외 규칙 자체를 건너뛰고 무조건 `type:'consecutive'`로 등록되어, 연참 우선순위로 관외비율 무시하고 자동승급될 수 있었음 |
| 수정 | `isConsecutiveCase` 분기 안에서, 종합운동장+관외 회원이면 연참대기 등록 전에 관외 규칙(관내최소3명, 비율)을 먼저 체크 — 미충족시 `type:'gwae_wait'`로, 충족시에만 기존대로 `type:'consecutive'`로 등록 |
| 검증 | 관내2명(미충족→관외대기)/관내3명(충족→연참대기 정상등록) 시뮬레이션으로 분기 정확성 확인 |

## 79. 유니온 새벽테니스 — 3번코트 확보란 신설 (v108, 2026-08-05)

| 항목 | 규칙 |
|---|---|
| 템플릿 변경 | `daily_union_morning` courts를 `["4번"]` → `["4번","3번"]`로 변경 — 4번 고정 + 3번 추가확보 구조 |
| 정원 계산 | `computeCapacity`가 courts.length===2로 자동 인식해 `includesCourt3=true` 판정, 저녁 유니온과 동일 공식 적용: 미확보시 (2-1)×8=8명, 확보시 2×6=12명 (시뮬레이션 검증 완료) |
| 3번코트확인팝업 예외 | `checkCourt3ConfirmPopup`에 `ev.start_time !== '06:00'` 조건 추가 — 낮12시 발동 시점엔 새벽 일정이 이미 끝나있어 뒤늦게 물어보는 게 부적절, 새벽은 전용 함수(`checkDawnCourt3Reminder`, 종료 1시간 이내 참석자에게 리마인드)가 이미 담당 중이므로 중복 방지 목적도 겸함 |
| 화금확률안내 예외 | `checkCourt3LikelyConfirm`에 `ev.start_time === '06:00'` 제외 조건 추가 — "새벽레슨 인원 때문에 확보확률 높다"는 근거가 새벽 자체 일정에는 논리적으로 성립 안 함(자기 자신이 새벽레슨인데 자기 때문에 확보된다는 순환논리 방지) |
| ⚠️ 부수효과 미해결 | 성별유연승급(`checkGenderFlexPromotions`, court_count===2 전용)이 이제 새벽테니스도 대상에 포함됨 — 별도 요청 없어 그대로 둠, 필요시 추가 제외 조건 요청 가능 |

## 80. 새벽코트 특수정원(6→8) (v108, 2026-08-05)

| 항목 | 규칙 |
|---|---|
| 배경 | 새벽코트는 레슨생(랠리 위주)과 게임조가 섞여있어, 저녁 유니온과 같은 표준 공식(8→12)이 아닌 더 작은 규모로 별도 지정 필요 |
| 구현 | `computeCapacity`에 `isDawnCourt = t.startTime==='06:00' && t.endTime==='08:00'` 판정 추가, 이 경우 `quota=8`(확보시), `unconfirmedMax=6`(미확보시)로 하드코딩 — courts.length 기반 표준공식을 타지 않음 |
| 검증 | 시뮬레이션으로 미확보(6명)/확보(8명) 정확히 계산됨 확인 |

## 81. 새벽코트 준관리자 (v108, 2026-08-05)

| 항목 | 규칙 |
|---|---|
| 데이터 필드 | `state.data.dawnSemiAdmins`(배열, 이름 목록) 신설 — defaultData·MERGE_ARRAY_FIELDS에 추가 |
| 헬퍼 함수 | `isDawnSemiAdmin(name)`, `canConfirmCourt3(ev)` — 관리자이거나(새벽일정+새벽준관리자)이면 true |
| 권한 확장 | "3번코트 확보 확인" 버튼의 표시조건을 `hasAdminPower()` → `canConfirmCourt3(ev)`로 교체 — 새벽 일정에서만 새벽준관리자도 버튼 노출 |
| 알림 문구 구분 | `toggleCourt3Confirmed`에서 `roleLabel = hasAdminPower() ? '관리자' : '새벽코트 준관리자'`로 알림 문구 분기 |
| 지정 UI | 회원관리 목록에 "🌅새벽준관리자 지정/해제" 버튼 추가, 지정된 회원은 이름 옆에 "🌅준관리자" 태그 표시 |
| 첫 지정 대상 | 홍진경(요청자 지정 예정) — 회원관리에서 관리자가 직접 버튼 클릭으로 지정 |

## 82. 새벽테니스 3번코트 — 서버데이터 마이그레이션 (v109, 2026-08-06)

| 항목 | 내용 |
|---|---|
| 발견 경위 | v108 배포(버전번호로 확인) 후에도 실사용 화면에서 새벽테니스 카드에 3번코트 관련 UI(안내문구·확보버튼)가 전혀 안 보임, "1개 코트·정원8명(최대8명)" 예전 표시 그대로 |
| 근본 원인 | **`buildTemplates()`는 데이터가 아예 없을 때(신규 셋업)만 쓰이는 초기값 생성 함수** — 이미 운영 중인 클럽은 Supabase `ignis_state`에 저장된 실제 템플릿 배열을 그대로 불러와 쓰기 때문에, 코드의 `buildTemplates()`를 수정해도 이미 저장된 서버 데이터(`daily_union_morning` courts:["4번"])는 자동 갱신되지 않음. `templates` 병합로직도 "dirty로 마크된 id만 로컬값 반영"이라 이 케이스를 안 잡아줌 |
| 수정 | `init()` 함수 초입, `state.data.templates` 로드 직후에 마이그레이션 코드 추가 — `daily_union_morning` 템플릿의 courts가 '3번'을 포함하지 않으면 `['4번','3번']`으로 갱신 후 `markDirty`+`saveData()`로 서버에 즉시 반영 |
| 교훈 | **코드 수정 ≠ 운영 데이터 반영.** JSON blob 형태로 서버에 영속화되는 설정값(템플릿, 각종 state.data 필드)을 코드에서 변경할 때는, 이미 저장된 기존 데이터를 어떻게 새 값으로 옮길지(마이그레이션)를 항상 별도로 고려해야 함. 앞으로 템플릿 구조를 바꿀 때마다 이 패턴(1회성 init-time 마이그레이션)을 재사용 가능 |

## 83. 화면 가로밀림 — 근본 안전장치 추가 (v110, 2026-08-06)

| 항목 | 내용 |
|---|---|
| 증상 | 스크롤하다보면 화면 전체가 옆으로 밀려서 하단 네비 등이 잘려 보임(스크린샷 확인) |
| 원인 | `body` 태그에 `overflow-x:hidden`이 애초에 없었음 — 즉 어떤 요소든(카드 내부 텍스트, 배지, 버튼 등) 화면 너비를 살짝만 초과해도 브라우저가 전체 페이지를 가로로 스크롤 가능하게 만들어버리는 구조 |
| 수정 | `html`, `body` 양쪽에 `overflow-x:hidden` + `max-width:100vw`(body) 추가 |
| 근본 원인 미특정 | 정확히 어떤 요소가 너비를 초과시켰는지는 특정하지 못함(district-line, rank-tabs 등 유력 후보 점검했으나 확정 못함) — 다만 이 안전장치로 증상 자체는 근본 차단됨 |
| 후속 확인 필요 | "하단 제일아래페이지가 초반에 먼저뜬다"는 두번째 제보는 원인 불명확 — 사용자에게 재현 상황 추가 확인 요청 중 |

## 84. 페이지 접속시 맨아래로 스크롤 — 수정 (v110, 2026-08-06)

| 항목 | 내용 |
|---|---|
| 사용자 확인 | "페이지 접속하면 제일아래로 내려가있음" — 83번 항목의 후속 제보로, 별개의 원인으로 확인됨 |
| 추정 원인 | `history.scrollRestoration` 기본값(auto)에 의존하고 있었음 — 캘린더·일정목록이 비동기로 순차 렌더링되며 페이지 높이가 계속 변하는 도중에, 브라우저가 "이전 스크롤 비율"을 복원하려다 엉뚱한(대개 맨 아래) 위치로 스크롤시키는 전형적인 SPA 패턴 버그 |
| 수정 | `history.scrollRestoration = 'manual'` 설정 + `window.scrollTo(0,0)`을 스크립트 최상단, `init()` 시작 시점, `renderAll()` 직후(전체 렌더링 완료 시점) 3곳에서 명시적으로 호출 |

## 84. 공불출내역 — 본인 기록 삭제 (2026-08-06)

| 항목 | 규칙 |
|---|---|
| 표시 조건 | `admin || b.member_name === state.myName` — 관리자이거나 본인이 등록한 기록이면 삭제 버튼 노출 |
| 재고 복원 | 삭제 대상이 `ball_type==='클럽볼'`이면, 삭제 전 확인창에 복원될 개수 안내 + 실제 삭제 후 `state.data.ballStock[location]`에 count만큼 되돌려줌 |
| 배경 | 회원이 실수로 중복 체크했을 때 관리자에게 매번 요청하지 않고 스스로 정정 가능하게 |

## 85. 공불출 삭제버튼 클릭무반응 — 긴급수정 (v112, 2026-08-06)

| 항목 | 내용 |
|---|---|
| 버그 원인 | 84번 항목(본인기록 삭제) 구현시, 삭제 클릭핸들러가 `list.find(b => b.id === id)`를 사용했는데, 이 코드가 실제로는 `renderBallPanel()`이 아니라 `wireBallSection()`이라는 별도 함수 안에 있었음 — `wireBallSection()`은 매개변수를 받지 않아 `list`에 접근 불가, 클릭시 "list is not defined" 런타임 에러 발생 |
| 발견 경위 | 사용자가 본인이 방금 등록한 클럽볼 기록 삭제를 시도했으나 "클릭 자체가" 안 된다고 제보 — 실제로는 클릭은 되지만 내부 에러로 조용히 실패 |
| 수정 | `wireBallSection(list)`로 매개변수 전달받도록 변경, 호출부(`renderBallPanel` 내부)도 `wireBallSection(list);`로 함께 수정 |
| 검증 | Node.js로 수정 전 코드 구조 재현 → "list is not defined" 에러 확인, 수정 후 구조로 재현 → 정상 조회 확인 |
| 교훈 | 새 기능을 "렌더 함수" 안에 바로 추가하는 대신, 실제로 그 코드가 어느 함수(render vs wire, 서로 다른 스코프) 안에 들어가는지 항상 먼저 확인할 것 — 렌더링과 이벤트 바인딩이 분리된 구조에서는 특히 변수 스코프를 재확인해야 함 |

## 86. 공불출내역 — 월간 재고정산 리포트 (v113, 2026-08-06)

| 항목 | 규칙 |
|---|---|
| 위치 | 공불출내역 패널, "전체초기화" 버튼 위에 "📊 월간 재고정산 리포트" 버튼(관리자 전용) |
| 계산 함수 | `computeBallMonthlyReport(list, days=30)` — cutoff(30일전) 이후 기록만 필터링, 위치·종류별/회원별/일자별로 집계 |
| 표시 내용 | 현재 재고(위치별+충전일), 최근30일 클럽볼 총반출, 위치·종류별 반출량, 회원별 TOP10, 일자별 추이 |
| CSV 다운로드 | `downloadBallCsv(recent)` — 날짜,회원,위치,종류,개수,사유 컬럼으로 BOM 포함 CSV 생성(엑셀 한글깨짐 방지), Blob+객체URL로 클라이언트에서 직접 다운로드 |
| 스코프 주의 | 85번 버그의 재발 방지 위해, `list`를 모든 관련 함수(`renderBallMonthlyReport`, `computeBallMonthlyReport`, 버튼 클릭핸들러)에 명시적으로 매개변수 전달 — 시뮬레이션으로 30일 필터링·집계 정확성 확인 완료 |
| 데이터 소스 | 기존과 동일하게 클라이언트가 이미 불러온 `list`(ignis_ball_checkouts 조회 결과) 활용 — 별도 DB 직접조회 없음 |

## 87. 늦참(N분) 표시 승급후 유실 버그 수정 (v114, 2026-08-06)

| 항목 | 규칙 |
|---|---|
| 버그 원인 | 대기(waiting)에서 참석(attend)으로 승급되는 코드 지점이 총 9곳 존재하는데, 그중 5곳이 새 vote 객체를 만들 때 `lateArrival`/`lateMinutes` 필드를 빠뜨림 — 늦참으로 등록됐다가 자동/수동 승급되면 "(늦N분)" 표시가 사라짐 |
| 수정한 5곳 | `tryPromoteGwaeWaiting`(관외대기승급), `checkEmergencyRequestTimeout`(7분자동승인), `approveGwaeReview`(관외 관리자승인), `approveLateRequest`(긴급참석 관리자승인), `adminMoveMember`의 attend-경로(관리자 수동이동, `existing.lateArrival` 참조) |
| 일관성 보완 | `setVote`의 히든석(운영진 오버플로우) 등록 지점에도 `lateArrival:false, lateMinutes:null` 명시적 추가 — 9곳 전부 필드 일관성 확보 |
| 검증 | 전체 코드에서 `status:'attend'` 생성 지점 전수 검색으로 누락 여부 재확인(2차), 승급 전/후 시뮬레이션으로 "(늦60분)" 표시 유지 확인 |

## 88. 승급 관련 함수 전수감사 — 관외비율 우회 가능성 수정 (v115, 2026-08-06)

| 항목 | 규칙 |
|---|---|
| 계기 | 사용자가 "자동승급이 기존규칙을 무시하는 사례가 더 있는지" 질문 — 승급 관련 함수 11개(`tryPromoteGwaeWaiting`, `checkConsecutiveWaitingLatePromotion`, `checkEmergencyRequestTimeout`, `checkGenderFlexPromotions`, `promoteGenderFlexWaiting`, `tryAutoPromote`, `runAutoPromotions`, `approveGwaeReview`, `approveLateRequest`, `promoteWaitingForCapacity`, `forcePromoteWaitingNow`, `adminForcePromote`) 전수 점검 |
| 발견한 구조적 허점 | 관외 회원이 "비율은 여유 있지만 전체정원이 꽉 차서" `type:'regular'`로 대기하게 되는 케이스가 존재 — 이후 이 사람이 `tryAutoPromote`(낮12시)/`promoteWaitingForCapacity`(정원확대)/`checkConsecutiveWaitingLatePromotion`(연참1시간전)/`promoteGenderFlexWaiting`(성별유연)/`forcePromoteWaitingNow`(관리자수동)로 승급될 때, 5곳 전부 관외비율(gwaeLimit)·관내최소3명 규칙을 재확인하지 않고 그대로 승급시키는 구조였음 |
| 수정 | 공통 헬퍼 `canPromoteRespectingGwaeLimit(ev, counts, v)` 신설 — 종합운동장+관외인 경우만 (관내최소3명 충족 && 비율여유) 체크, 관내이거나 유니온이면 항상 true. 5개 함수 전부에 적용: `tryAutoPromote`/`forcePromoteWaitingNow`는 순회 루프에서 `continue`로 스킵, `promoteWaitingForCapacity`/`checkConsecutiveWaitingLatePromotion`/`promoteGenderFlexWaiting`은 `waiting[0]` 대신 `waiting.find(...)`로 조건 통과하는 첫 대기자를 탐색 |
| 검증 | 관내/관내최소3명미충족/비율여유/비율꽉참/유니온(관외규칙무관) 5개 케이스 시뮬레이션 전부 정확 |
| 부가 확인 | "참석6+대기1" 질문에 대한 답변 — 대기는 본인이 클릭한 시각(ts) 기준으로 우선순위가 고정되며, 이후 다른 회원의 참석 등록은 기존 대기자 우선순위에 영향 없음(정상 동작 확인, 별도 수정 불필요) |

## 89. 늦참(N분) 표시 안 되던 진짜 원인 2건 수정 (v115, 2026-08-06)

| 항목 | 내용 |
|---|---|
| 배경 | v114에서 승급함수 5곳의 lateArrival 보존을 고쳤음에도 "여전히 안 보인다"는 재제보 — 승급함수 11개 전체를 재검증했으나 전부 정상이었음, 근본 원인은 승급 로직이 아니라 화면표시 단계에 있었음 |
| 원인① | 메인 참석카드 "대기(N)" 이름목록(`renderNameChips` 호출부)에서 6가지 대기유형만 나열하고, 최근 신설한 `waitLateNames`(늦참대기)가 이 목록 자체에서 누락 — 카운트엔 포함되지만 이름은 안 보이는 "사라짐" 현상의 정체. 목록에 `renderNameChips(counts.waitLateNames,' (늦참)',...)` 추가 |
| 원인② | `waitLateNames`/`waitConsecutiveNames`(late_consecutive 케이스)의 entry 객체 자체에 `lateArrival` 필드가 없어서, `lateTag` 조건(`e.lateArrival`)이 항상 false — 두 목록 모두 entry 생성시 `lateArrival:true` 명시적으로 포함하도록 수정 |
| 검증 | 5분/45분/60분 등 다양한 분 단위로 즉시참석·대기중·승급후 3단계 시뮬레이션 전부 정상 확인. 30분 이상만 표시되는 제한 없이 전체 분 단위 표시됨(요청사항 반영) |
| 교훈 | 새 대기유형(waitLateNames)을 만들 때, 데이터 집계(computedCounts)만 고치고 실제 화면에 뿌리는 지점(메인카드 이름목록)을 함께 안 고치면 "데이터는 있는데 안 보이는" 버그가 생김 — 새 카테고리 추가시 표시 지점 전체를 체크리스트로 확인할 것 |

## 90. 늦참(N분) 표시 — 최종 근본원인: DB 저장 자체가 누락 (v116, 2026-08-06)

| 항목 | 내용 |
|---|---|
| 배경 | v115 배포 후에도 재현된다는 스크린샷 제보 — 승급함수·화면표시 로직은 전부 정상인데도 안 됨. `saveVoteRow`(DB저장 함수) 자체를 재검사 |
| 진짜 원인 | `saveVoteRow`가 Supabase `ignis_votes` 테이블에 upsert할 때 필드 목록에 `late_arrival`/`late_minutes`가 애초에 빠져있었음 — 로컬 메모리(state.data.votes)에는 정상 저장되어 화면엔 잠깐 보이지만, **서버에는 저장되지 않아** 새로고침·재접속·실시간동기화(다른 회원 화면)에서 전부 사라지는 구조적 문제. v114/v115에서 고친 것들은 전부 "로컬 처리" 단계였고, 이번이 진짜 마지막 조각 |
| 수정한 3곳 | ① `saveVoteRow`(저장) — upsert 객체에 `late_arrival: !!voteObj.lateArrival, late_minutes: voteObj.lateMinutes \|\| null` 추가 ② `loadData`(초기 로딩) — `lateArrival: !!row.late_arrival, lateMinutes: row.late_minutes \|\| null` 매핑 추가 ③ Supabase realtime 구독 콜백 — 동일하게 `payload.new.late_arrival`/`late_minutes` 매핑 추가 |
| ⚠️ DB 마이그레이션 필요 | Supabase `ignis_votes` 테이블에 `late_arrival`(boolean), `late_minutes`(integer) 컬럼이 없으면 upsert 자체가 실패할 수 있음 — `SQL_늦참컬럼추가.sql` 1회 실행 필요: `ALTER TABLE ignis_votes ADD COLUMN IF NOT EXISTS late_arrival boolean DEFAULT false, ADD COLUMN IF NOT EXISTS late_minutes integer DEFAULT NULL;` |
| 검증 | 저장→로딩 왕복(round-trip) 시뮬레이션으로 lateArrival/lateMinutes가 DB를 거쳐도 정확히 보존되는 것 확인 |
| 교훈 | 새 필드를 votes 객체에 추가할 때, "로컬 상태 처리"와 "DB 영속화(저장+로딩+realtime)"는 완전히 별개의 체크 대상 — 로컬에서 잘 되는 것처럼 보여도 DB 왕복까지 반드시 확인해야 함 |

## 91. "로컬처리 vs DB저장" 유사사례 전체 전수점검 (v117, 2026-08-06)

| 항목 | 내용 |
|---|---|
| 계기 | 90번(lateArrival DB저장누락) 사건 직후, 같은 유형의 버그가 다른 곳에도 있는지 전체 점검 요청 |
| 방법 | ① `votes[...] = {...}` 전체 패턴에서 실제 사용되는 필드 정규식 추출 → `saveVoteRow` 저장목록과 대조 ② `state.data.XXX` 사용 필드 전체 → `defaultData()` 정의 필드와 대조 ③ MERGE_MAP_FIELDS 대상 필드의 `state.data.FIELD[key]=` 대입 지점 전수 검색 → 인근에 `markDirty` 호출 여부 확인 |
| 발견① | `autoApprovedPopupPending`(7분 자동승인 팝업 안내용) — votes 저장목록에서 누락. DB에 `auto_approved_popup_pending` boolean 컬럼 추가(Supabase MCP로 직접 실행), 저장·로딩·realtime 3곳 매핑 완료 |
| 확인② | `state.data`(JSON blob) — defaultData의 모든 필드가 MERGE_MAP_FIELDS/MERGE_ARRAY_FIELDS/특별취급(templates·extraMembers) 중 하나로 전부 커버됨, 누락 없음 |
| 확인③ | markDirty 호출 누락으로 보였던 3건(전부 notifiedFlags) — 실제로는 "다른 세션이 서버에 이미 저장한 값을 로컬에 동기화만 하는" 의도된 정상 로직(재확인 후 저장 스킵), 오탐으로 확인 |
| 확인④ | comments/notifications/warnings/penalties/ball_checkouts — 전부 별도 정규 테이블(`ignis_comments` 등)로 완전히 분리되어 있어 이 유형 문제와 무관 |
| 실행 방식 | Supabase MCP(`apply_migration`, `execute_sql`) 연동으로 컬럼 추가·검증을 대화 중 직접 실행 — 더 이상 별도 SQL 파일을 사용자가 수동 실행할 필요 없음 |

## 92. 관리자모드 "이동▾" 버튼 화면밖 이탈 버그 수정 (다음 배포분)

| 항목 | 내용 |
|---|---|
| 증상 | 이름+태그(승급라벨·관외·시간·늦N분 등)가 많이 붙은 참석자의 경우, 관리자모드 "이동▾" 드롭다운이 화면 오른쪽 밖으로 밀려나서 클릭 자체가 불가능 |
| 근본 원인 | `.name-chip`에 `white-space:nowrap`이 있어 칩 내부 모든 요소가 한 줄에 강제로 붙어 있으려 했음. 여기에 79번 항목에서 이미 추가된 `overflow-x:hidden`(다른 문제 방지용)이 겹치면서, 화면 밖으로 나간 부분에 가로스크롤로도 접근 불가능해지는 조합 문제 |
| 수정 | `.name-chip`에서 `white-space:nowrap` 제거, `flex-wrap:wrap` + `box-sizing:border-box` 추가 — 내용이 많으면 칩 내부에서 자연스럽게 줄바꿈되며 항상 부모 컨테이너(화면) 안에 표시됨 |
| 검증 방법 | 독립 HTML을 wkhtmltoimage로 렌더링해서 수정 전(파란 테두리=화면 영역 밖으로 이동버튼 이탈)/후(테두리 안에서 정상 wrap) 시각적으로 직접 비교 확인 |
| 파급 확인 | `.name-tags`(부모, 이미 flex-wrap 보유)와 `.attend-box`(최상위 컨테이너)에 `overflow:hidden`이나 고정 높이가 없어, 칩이 여러 줄이 되어도 박스 자체가 자연스럽게 세로로 늘어남 — 다른 레이아웃 깨짐 없음 확인 |

## 93. 늦참(30분 초과, 60분+ 포함) 신규 등록 조용한 실패 방어 처리 (v119, 2026-08-08)

| 항목 | 내용 |
|---|---|
| 제보 | "기존 늦참자가 60분 이상 늦참일 경우 대기에서 안 보여지는 버그" — 방금 새로 60분+로 등록했는데 메인 참석카드 "대기(N)" 목록에 이름이 안 뜸 |
| 코드 재검토 결과 | `computedCounts`/`renderNameChips`(대기 목록 렌더링 지점) 자체는 87~90번에서 이미 수정되어, `type:'late'`로 정상 저장만 되면 60분이든 몇 분이든 필터링 없이 전부 표시되는 구조였음(재확인 완료) |
| 실제 위험 지점 | `checkLateArrival()`이 `try/catch` 없이 작성되어 있어, 등록 과정 중 어느 한 단계(예: `isConsecutiveCase` 연속참석 판정, `saveVoteRow` 저장 등)에서 예외가 나면 **사용자에게 아무 안내 없이 등록 자체가 중단**되던 구조 — 안 보이는 게 아니라 애초에 등록이 안 된 것일 가능성이 높음 |
| 수정 | ① `checkLateArrival()` 전체를 `try/catch`로 감싸 실패 시 오류 메시지를 사용자에게 alert로 안내 ② `isConsecutiveCase` 예외는 등록 전체를 막지 않고 일반 늦참(`type:'late'`)으로 대체 처리되도록 격리 ③ 직접입력(예: "60") 파싱을 숫자만 추출하도록 보강, 300분 초과 등 비정상 값 사전 차단 |
| 부가 수정 | "오늘의 상황보고" 패널(`renderTodayPanel`)과 일일 결산 "끝까지 대기하다 승급 못하신 분" 목록(`buildTodaySummary`)에서 늦참대기·긴급요청·성별대기·선마감대기 카테고리 일부가 누락되어 있던 것도 함께 보강 — 메인 대기목록과는 별개 화면이지만 같은 유형의 "데이터는 있는데 특정 화면에서만 안 보이는" 버그였음 |
| ⚠️ 참고 | "참석 승급됐는데 화면에 이름 자체가 안 뜸" 건은 승급 관련 함수(87~88번에서 이미 전수 점검됨) 및 gender/hidden 필드 처리 코드를 재검토했으나 이번 세션에서는 정적 분석만으로 추가 원인을 찾지 못함 — 재현되면 콘솔 오류 로그와 함께 다시 제보 요망 |
| 부가 발견 | Supabase `ignis_votes` 실데이터 점검 중, 관리자가 "이동▾ → 대기로(늦참)"으로 수동 이동시키는 경로(`adminMoveMember`의 `waiting-late`)에서 `lateArrival`/`lateMinutes` 필드가 아예 비어있는 기존 사례(예: 주소연) 발견 — 회원 본인이 늦참 버튼으로 등록한 경우와 달리 관리자 수동이동 경로에서만 누락되어 있었음. 해당 경로에도 필드 채우도록 수정 |
| 검증 | 스크립트 블록 3개 전체 `node --check` 문법 검증 통과, Supabase `ignis_votes` 라이브 데이터 조회로 실제 문제 사례 확인 |

## 94. 승급됐는데 화면에 안 보이던 버그 — 진짜 원인 규명: 관리자 실시간동기화 지연 + 수동보정 데이터유실 (v120, 2026-08-08)

| 항목 | 내용 |
|---|---|
| 제보 | 스크린샷(8/7 종합운동장 밤) 근거로 "늦참 누락은 최윤하님"으로 구체 지목 |
| 조사 방법 | Supabase `ignis_votes`/`ignis_comments` 라이브 데이터를 타임스탬프 단위로 직접 대조하여 실제 이벤트 순서 재구성 |
| 실제 순서 | ① 09:10:26.897 최윤하님 60분 늦참 등록(대기) ② 09:10:26.899(2ms 후) 다른 기기의 주기적 자동승급(`runAutoPromotions`, 20초 간격)이 `tryAutoPromote`로 정상 승급(lateArrival 보존, promotedFrom:'wait') ③ 09:10:27.075 본인 화면에도 참석 처리 확인 |
| 진짜 문제 | 관리자 화면은 실시간 동기화가 지연되어 여전히 "안 보임"으로 표시됐을 가능성이 높고, 이를 본 관리자가 "이동▾→여자참석으로→늦참승급"으로 수동 재처리 — 이때 관리자 화면의 로컬 캐시(`existing`)가 최신이 아니어서(자동승급 반영 전 상태) `lateArrival`/`lateMinutes`가 `false`/`null`로 덮어써짐. 실제 DB에 `promoted_from:'late', late_arrival:false`로 남아있던 것으로 확인 |
| 수정 | `adminMoveMember()` 시작 시, 로컬 `state.data.votes` 캐시 대신 Supabase에서 해당 회원의 현재 행을 직접 재조회(`select('*').eq('event_id',...).eq('member_name',...)`)해서 `existing`으로 사용하도록 변경 — 관리자 화면이 뒤처져 있어도 수동 이동 시 기존 정보 유실 방지 |
| 데이터 복구 | 최윤하님 8/7 종합운동장 밤 기록을 `late_arrival=true, late_minutes=60`으로 Supabase에서 직접 UPDATE로 정정 |
| 남은 과제 | 근본적으로는 "관리자 화면 실시간 반영 지연" 자체가 원인이므로, 추후 재현되면 realtime 구독 연결상태(재연결 로직)나 20초 폴링 간격을 단축하는 방향도 검토 필요 |

## 95. 새벽테니스 — 성별 상한(성별대기) 완전 제외 (v121, 2026-08-09)

| 항목 | 내용 |
|---|---|
| 요청 | "새벽 코트는 성별대기 삭제해줘" — 8/11 유니온 새벽테니스에서 여자 참석 3명 도달로 이빛나님이 "성별대기"로 전환된 사례 확인 후 요청 |
| 확인한 확답 | ① 성별 상한 자체를 없애고 전체정원으로만 대기처리 (한쪽 성별이 다 차지해도 가능) ② 적용범위: 새벽테니스(06:00~08:00) 전체 일정 |
| 수정 | `isDawnEvent(ev)` 공통 헬퍼 신설(`start_time==='06:00' && end_time==='08:00'`). 아래 지점 전부에 새벽코트 예외 처리: ① 참석 등록시 성별상한 체크(성별대기 전환 로직) ② `tryAutoPromote` ③ `promoteWaitingForCapacity` ④ `forcePromoteWaitingNow` 의 대기자 필터 ⑤ `checkGenderFlexPromotions`(성별 유연승급 판정 자체를 새벽코트는 스킵) ⑥ `adminForcePromote`의 flex 판정 ⑦ 관리자 "이동▾" 드롭다운에서 새벽코트는 "대기로 (성별대기)" 옵션 숨김 |
| 영향 없음 확인 | 저녁/유니온 등 기존 2코트 일정(예: tue_union_night)은 그대로 성별대기 로직 유지 — Supabase 라이브 데이터로 새벽코트 쪽엔 남은 gender_wait 대기건이 없는 것도 확인 |
| 검증 | 스크립트 3블록 `node --check` 통과 |

## 96. 3번코트 확인 팝업 — 이미 끝난 일정에도 밤늦게 뜨던 버그 수정 (v122, 2026-08-09)

| 항목 | 내용 |
|---|---|
| 제보 | "일요일 12-14 유니온" 일정(정오~오후2시)인데, 밤 9시 반(21:30)에 "3번코트가 확보되었나요?" 팝업이 뜸 — 이미 끝난 일정을 뒤늦게 물어보는 게 무의미 |
| 원인 | `checkCourt3ConfirmPopup()`이 `now.getHours() < 12`(낮 12시 이후 전체)만 조건으로 걸어놨음 — 그날 앱을 몇 시에 처음 열든(자정 직전이라도) 계속 물어보는 구조. 일정 시작/종료 시각과 전혀 연동되지 않았음 |
| 수정 | 일정 시작 2시간 전 ~ 시작 전 구간에서만 발동하도록 변경 (`checkSaturdayCourt3Reminder`의 "시작 1시간전" 패턴과 동일한 방식, 여기선 2시간). 요일 구분 없이 3번코트 포함 모든 일정에 동일 적용 — 원래 버그가 요일과 무관했기 때문 |
| 검증 | 스크립트 3블록 `node --check` 통과 |

## v123. dothome 로딩 실패 — 3분할 파일 크기 불균형 원인 파악 및 재분할

| 항목 | 내용 |
|---|---|
| 제보 | v122 업로드 후 사이트 로딩이 안 됨 |
| 조사 방법 | header+body+footer 원본 파일을 그대로 이어붙여 전체 검증(JS구문 node --check, ID참조, 중복함수, MEMBERS파싱, body/html 태그 짝) — 전부 정상, 원본의 실제 분할 경계도 함수 정의 시작/끝 지점에서 깔끔하게 나뉘어 있어 로직적 결함 없음 확인 |
| 발견한 원인 | 파일 크기가 header 340KB · body 230KB · footer 138KB로 심하게 불균형(약 45%/31%/24%) — 원래 원칙(33/33/33 균등분할)에서 크게 벗어나 있었음. header가 유독 커서 dothome 업로드·로딩 과정에서 문제가 됐을 가능성이 높음 |
| 조치 | 표준 빌드로직(마커 기반 균등분할)으로 재구성 — header 136KB · body 255KB · footer 200KB로 재조정. 코드 내용은 원본과 100% 동일(공백 제외 완전 일치 확인) |
| 빌드스크립트 버그 발견·수정 | 재분할 과정에서, footer 뒷부분(`</script>` 이후의 `</body></html>`)을 빠뜨릴 뻔한 걸 자체 검증(`assembled.rstrip().endswith("</html>")`)으로 잡아냄 — 표준 빌드스크립트에 `after_script` 변수로 이 부분을 명시적으로 보존하도록 보강. 앞으로도 이 태그 누락 여부를 배포 전 필수 체크 항목에 추가 |
| 교훈 | 3분할 파일을 만들 때 "코드 내용이 맞는가"뿐 아니라 "세 파일의 크기가 균형 잡혀 있는가"도 반드시 함께 확인해야 함 — 특정 파일이 과도하게 크면 호스팅 플랫폼의 파일 크기 제한에 걸려 로딩 자체가 실패할 수 있음 |

## v124. 새벽준관리자 "해제" 불가 버그 + 회원관리 저장로직 전체 재점검

| 항목 | 내용 |
|---|---|
| 제보 | 새벽준관리자 지정은 되는데 해제가 안 됨 |
| 근본원인 | `dawnSemiAdmins`가 배열+"합집합 전용" 병합(MERGE_ARRAY_FIELDS)에 있어, 해제해도 저장시 서버 예전값과 합쳐지며 원상복구되는 구조 |
| 증거 | 실제 Supabase 조회 결과 10명이 계속 누적되어 있었음 |
| 수정 | dawnSemiAdmins를 배열→객체(map)로 전환, MERGE_MAP_FIELDS(dirty-key 기반 정확병합)로 이동. 해제는 null 설정 |
| 연쇄발견1 | removedMembers(탈퇴/복구)도 동일 문제 + markDirty 3곳 누락 → 동일하게 객체방식+markDirty 추가 |
| 연쇄발견2 | delete 직접사용 3곳(notifiedFlags, wholeCancelled, pendingApproval) → 병합로직과 안 맞는 위험코드, null설정으로 안전화 |
| 연쇄발견3(버그아님) | penalties/warnings가 JSON blob에도 저장되고 있었으나, 실제로는 정규테이블에서 매번 재구성되는 죽은데이터 확인 → 저장대상서 제외 |
| DB작업 | Supabase 직접연동으로 기존 배열데이터(새벽준관리자10명, 탈퇴회원2명) 객체로 마이그레이션, memberPins/fingerprintChecks/votes 실데이터 정상저장 확인 |
| 파일크기 | header 136KB / body 257KB / footer 201KB로 균형 유지, `</body></html>` 보존 확인 |

## v125. "새벽코트 준관리자" → "VIP3"로 확장 개편

| 항목 | 규칙 |
|---|---|
| 명칭 | dawnSemiAdmins(배열/객체) → vip3Members(객체) 완전 전환, 화면표시도 전부 "VIP3"로 |
| 범위확장 | canConfirmCourt3(ev): 기존 `ev.start_time==='06:00'` 조건 제거 → `ev.includesCourt3`(새벽 포함 유니온 3번코트 전체 일정)로 확장 |
| 운영진 포함 | `state.myIsStaff`(★스태프) 체크 추가 — 운영진은 별도 지정 없이 자동으로 모든 3번코트 일정 확인 가능 |
| 실시간 확인 강화 | `checkCourt3ConfirmPopup`의 대상 조건에 `hasAdminPower() OR state.myIsStaff OR isVip3(state.myName)` 추가 — VIP3/운영진은 해당 일정 참석 여부와 무관하게 항상 팝업 대상(기존엔 참석중이어야만 대상이었음) |
| UI | 회원관리에 "VIP3 지정/해제" 버튼(+설명 툴팁), 회원 이름 옆 "VIP3" 태그. 3번코트 확보 알림 문구도 관리자/운영진/VIP3 3단계로 세분화 |
| DB 마이그레이션 | Supabase 직접 연동 — 기존 dawnSemiAdmins 데이터(7명, v124 배포후 실제 해제 작동 확인됨 — 10명에서 7명으로 정상 감소) 그대로 vip3Members로 이전 |
| 검증 | 6가지 케이스(일반회원/VIP3-새벽/VIP3-저녁/운영진/관리자/VIP3인데3번코트아닌일정) 시뮬레이션 전부 정확 |

## v126. VIP3 지정시 환영팝업 신설 + 유사버그 재점검

| 항목 | 내용 |
|---|---|
| 재점검 결과 | VIP3 관련 코드(markDirty 5건 의심 재검색) 포함 전체 재검사 — 전부 정상, 추가 발견 없음(오탐 확인) |
| 신설 필드 | `vip3WelcomePending`(MERGE_MAP_FIELDS 추가) — VIP3 지정시 `true`로 설정, 팝업 표시 후 `null`로 삭제되는 1회성 플래그 |
| 신설 함수 | `checkVip3WelcomePopup()` — 본인이 vip3WelcomePending 대상이면 alert로 환영메시지 표시(권한설명+"다른회원이 확보해도 대신체크 가능"+감사인사), 표시 즉시 플래그 삭제+저장 |
| 호출 위치 | `init()`(최초 로드) + 20초 주기 setInterval(이미 접속중인데 지정된 경우도 곧 반영) 2곳 |
| 검증 | Node.js 시뮬레이션으로 "지정→1회 표시→플래그삭제→재접속시 다시 안뜸" 전체 흐름 확인 |

## v127. 운영진 접속시 자동 관리자모드 진입

| 항목 | 내용 |
|---|---|
| 요청 | 운영진(★스태프)은 접속하면 버튼 클릭 없이 바로 관리자모드로 시작, 버튼도 "켜짐" 상태로 표시 |
| 수정 | `applyMemberInfo(name)`에서 `m.is_staff`이면 `state.adminMode = true` 자동 설정 — 로그인(PIN확인후)과 자동재접속(init()) 양쪽 호출 지점 모두에서 이후 `syncAdminUI()`가 호출되어 버튼/패널 전부 자동 반영됨 |
| 버튼 스타일 | 별도 CSS 수정 불필요 — 기존 `.admin-toggle.on`(진한배경+흰텍스트) 스타일이 `syncAdminUI()`를 통해 자동 적용됨 |
| 동작 방식 | 수동으로 끄면 그 세션에서는 꺼진 채 유지, 재접속시 다시 자동으로 켜짐(의도된 동작) |
| 검증 | 운영진로그인/일반회원로그인/운영진재접속 3케이스 시뮬레이션 전부 정확 |

## v128. 운영진 지정/해제 기능 신설 (관리자 재량)

| 항목 | 내용 |
|---|---|
| 배경 | 기존엔 운영진(is_staff)이 MEMBERS 배열에 하드코딩되어 있어 관리자가 화면에서 바꿀 방법이 없었음 |
| 신설 필드 | `staffOverrides`(MERGE_MAP_FIELDS) — `{name: {on: true/false, role: "직책"}}` 구조 |
| 적용 로직 | `getActiveMembers()`에서 districtOverrides와 동일한 패턴으로 적용 — override 있으면 is_staff/role을 덮어씀, 해제(on:false)시 role도 명확히 빈 문자열로 처리 |
| 신설 함수 | `toggleMemberStaff(name)` — 지정시 직책 prompt 입력(선택), 해제시 확인 후 처리. 본인이 스스로를 변경한 경우 state.myIsStaff/myRole 즉시 갱신 + 운영진 지정시 state.adminMode도 즉시 true(v127 자동진입과 연동) |
| UI | 회원관리 목록에 "운영진 지정/★운영진 해제" 버튼 (VIP3 버튼 옆) |
| ⚠️ 개발중 자체발견 | 함수 삽입 과정에서 기존 `toggleMemberDistrict` 함수 선언부(`async function toggleMemberDistrict(name){`)가 실수로 통째로 사라지는 구문오류 발생 — 매 변경 후 필수인 `node --check` 구문검증에서 즉시 포착, 원인 특정 후 복구. 배포 전 항상 전체 검증을 거치는 프로세스가 실제로 오류를 걸러낸 사례 |
| 검증 | 지정/해제/미변경 3케이스 시뮬레이션 전부 정확, 최종 구문검증 통과 |

## v129. 1회성 이벤트 + 개별삭제3단계 + 월례대회 커스텀정원 + 회원참석랭킹

| 항목 | 규칙 |
|---|---|
| 1회성 이벤트 | templates에 `specificDate`(YYYY-MM-DD) 필드 지원 — generateInstances가 daysOfWeek 대신 이 날짜에만 매칭. 폼에 date input 추가 |
| 개별삭제 3단계 | `skipOccurrence(eventId)`를 showChoiceDialog 기반으로 확장 — 이날짜만(skippedDates)/이날짜부터이후(templates.endDate)/전체삭제(removedTemplateIds). 1회성이벤트는 "이 일정 삭제" 하나만(템플릿 자체 완전삭제) |
| endDate 지원 | generateInstances에 `if(t.endDate && iso > t.endDate) return;` 추가 — 과거 발생분은 유지하고 그 날짜 이후만 중단 |
| 월례대회 커스텀정원 | 1회성 이벤트에 한해 "정원 직접 지정" 입력 가능 — capacityOverrides[templateId_date]에 즉시 반영. computeCapacity에서 quota/max_players 둘 다 커스텀값으로 통일(표시 혼란 방지) |
| 회원참석랭킹 | `computeMemberRankingReport(start,end)` — getActiveMembers 전체 기준(참석0회 포함)으로 성별 정렬, 상위/하위 10명. `downloadMemberRankingCsv`로 CSV 다운로드. renderMonthlyMemberStats에 UI 통합(기존 이번달/이번분기/올해 기간선택 재사용) |
| 매월1일 알림 확장 | buildMonthlySummary에 남녀 상위3/하위3 요약 추가 |
| DB 변경 | 없음 — 전부 기존 ignis_state(JSON blob) 안의 templates/capacityOverrides 필드 확장이라 SQL 마이그레이션 불필요 |
| ⚠️ 개발중 반복실수 | 함수 삽입시 기존 함수 선언부가 사라지는 동일유형 실수 2건(toggleMemberDistrict, classifyEventCategory) 발생 — 매번 node --check로 즉시 발견·복구, 최종적으로 관련 함수 14개 전수 존재확인으로 재검증 |
| 검증 | 월례대회(3코트+커스텀40명), 회원랭킹(참석0회 포함 정확한 하위권), endDate/specificDate 매칭 전부 시뮬레이션 통과 |

## v130. 성별대기 우선순위버그 수정 + 이벤트카드 금빛스타일

| 항목 | 내용 |
|---|---|
| 버그 | waitingSortKey에서 gender_wait(성별대기)가 명시적으로 분류 안 되어 else분기로 consecutive(우선순위2)와 동급 취급 → regular(0)보다 항상 늦게 승급되던 문제. 8/13 김정현님 사례로 발견 |
| 수정 | `v.type === 'regular' \|\| v.type === 'gender_wait'`이면 우선순위 0으로 통일 — "정원부족으로 대기중"이라는 동일 성격이므로 순수 대기시각(ts) 선착순으로만 비교. 늦참(1)/연참(2)/늦참연참(3) 체계는 그대로 유지 |
| 영향범위 | waitingSortKey를 쓰는 모든 승급함수(tryAutoPromote/promoteWaitingForCapacity/promoteGenderFlexWaiting 등)에 자동 반영 |
| 검증 | 시나리오 재현(먼저대기한 성별대기가 이제 먼저 승급)+기존규칙유지(늦참/연참 순위 안 바뀜) 2가지 시뮬레이션 |
| 이벤트카드 스타일 | isSpecialEvent(1회성 이벤트) 필드를 generateInstances 출력에 추가, 카드에 special-event-card 클래스 부여 → 금빛 테두리(#d4af37)+은은한 그라데이션+"✨ 특별 이벤트" 뱃지로 시각적 구분 |

## v130. 개별일정 수정/삭제 버튼 위치개선 + 수정기능 신설

| 항목 | 내용 |
|---|---|
| 배경 | "개별삭제가 안 된다"는 피드백 — 실제론 있었지만 카드 중하단(정원조절 옆)에 있어 눈에 안 띄었음. "수정"(시간/코트변경) 기능은 애초에 없었음 |
| 버튼 위치 | 카드 최상단(제목 영역) 오른쪽에 "✏️ 수정"/"🗑️ 삭제" 고정 배치(관리자 전용) |
| 삭제 | 기존 skipOccurrence(3단계 선택) 그대로 재사용 |
| 수정(신규) | `editOccurrence(eventId)` — prompt 3연쇄(시작/종료시간, 코트)로 입력받아 `eventOverrides[eventId] = {startTime,endTime,courts}` 저장. `generateInstances`가 인스턴스 생성시 이 override를 템플릿 원본보다 우선 적용 — 그 날짜만 바뀌고 반복템플릿과 다른 날짜는 전혀 영향없음. 1회성 이벤트는 템플릿 자체가 그 일정 하나뿐이라 템플릿을 직접 수정 |
| 신설 필드 | `eventOverrides`(MERGE_MAP_FIELDS) |
| DB 변경 | 없음(JSON blob 확장) |
| 검증 | 함수 17개 전수 존재확인, override 적용/미적용/원본템플릿 불변 3케이스 시뮬레이션 |

## v131. 3번코트 확보시 성별대기 우선순위 버그 수정 + 특별이벤트 표시 확인

| 항목 | 내용 |
|---|---|
| 제보 | 3번코트 확보시, 성별대기로 먼저 등록한 사람보다 늦게 등록한 일반대기자가 먼저 승급됨 |
| 원인 | 2코트 템플릿(예: 2번,3번)에 3번코트 포함시, 확보되어도 court_count는 그대로 2 — 승급함수 4곳의 "court_count===2면 성별당6명 상한 넘는 승급 보류" 규칙이 확보여부와 무관하게 계속 적용되어, 성별대기자만 막히고 상한에 안 걸리는 일반대기자가 먼저 승급되던 구조적 문제 |
| 수정 | tryAutoPromote/promoteWaitingForCapacity/forcePromoteWaitingNow/adminForcePromote 4곳 모두 조건에 `&& !(ev.includesCourt3 && ev.court3Confirmed)` 추가 — 3번코트 확보시엔 이 상한 체크를 건너뜀, 미확보시엔 기존 규칙 유지 |
| 검증 | 확보/미확보 대조군 시뮬레이션으로 수정 전(버그 재현)/후(정상) 확인 |
| 특별이벤트 표시 | v129에서 이미 구현되어 있었음을 확인 — isSpecialEvent(1회성 이벤트)에 골드테두리+"✨특별이벤트" 배지 자동 적용. wkhtmltoimage로 실제 렌더링 확인(추가 작업 불필요) |

## v132. 3번코트 확보시 늦참+연참(late_consecutive) 잘못 자동승급되던 오류 수정

| 항목 | 내용 |
|---|---|
| 제보 | 3번코트 확보시, 늦참이면서 동시에 연참인 대기자가 원래 규칙(시작1시간전 전용경로로만 승급)을 무시하고 즉시 자동승급됨 |
| 원인 | promoteGenderFlexWaiting/tryAutoPromote/promoteWaitingForCapacity 3곳의 성수기 필터가 `v.type==='consecutive'`만 걸러내고 `late_consecutive`는 누락 — 코드 주석엔 "늦참+연참은 항상 전용경로로만 승급"이라는 설계의도가 이미 있었는데 실제 필터엔 반영 안 됨(checkConsecutiveWaitingLatePromotion의 2260번 주석 참고) |
| 수정 | 3곳 모두 `v.type==='late_consecutive'`는 계절 무관 항상 제외, `consecutive`는 기존대로 성수기에만 제외로 분리 |
| 검증 | 성수기/비수기 × 4개 타입(regular/late/consecutive/late_consecutive) 시뮬레이션 전부 정확 |

## v133. 승급함수 9개 전체 전수점검 — 선착순 원칙 일관성 재검증

| 항목 | 내용 |
|---|---|
| 요청 | 3번코트 확보로 인한 연참·늦참 우선순위 문제가 더 없는지 전체 점검, 선착순 원칙 위반 여부 확인 |
| 점검 대상 | tryPromoteGwaeWaiting/checkConsecutiveWaitingLatePromotion/promoteGenderFlexWaiting/tryAutoPromote/promoteWaitingForCapacity/forcePromoteWaitingNow/approveGwaeReview/approveLateRequest/checkEmergencyRequestTimeout (9개) |
| 결과 | waitingSortKey 정렬을 쓰는 7개 함수 전부 정상, 선착순 위반 없음. 개별승인 2개는 정렬 개념 자체가 무관 |
| 수정 | forcePromoteWaitingNow(관리자 수동실행)만 다른 6개와 달리 늦참+연참/성수기연참 제외 필터가 없어 일관성 위해 추가(선착순 자체는 원래도 정확했음) |
| 검증 | 5명(우선순위 뒤섞인 등록시각) 시나리오로 전체 정렬 최종 확인 — 그룹 내에서만 선착순 정확 적용 |

## v134. 3번코트 확보시 성별상한 완전무시되던 심각한 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 남6·여5로 남자쪽이 가득 찼는데도, 3번코트 확보 후 대기중이던 남자 2명이 둘 다 승급되어 남8·여5가 됨 |
| 진짜 원인 | v131에서 "3번코트 확보시 2코트기준 성별상한(6명) 체크를 건너뛴다"고 수정했는데, 이게 "코트수에 비례해서 상한을 늘리는" 게 아니라 "상한 체크 자체를 꺼버리는" 방식이었음 — 3번코트만 확보되면 사실상 무제한으로 한쪽 성별이 채워질 수 있었던 심각한 회귀버그(v131의 부작용) |
| 수정 | tryAutoPromote/promoteWaitingForCapacity/forcePromoteWaitingNow 3곳 모두 `genderCap = Math.ceil(ev.quota / 2)`로 상한을 quota(코트수×6)의 절반으로 비례 확대하도록 재설계. 2코트=상한6(기존유지), 3코트확보=상한9(비례확대, 무제한 아님) |
| 검증 | 수정전(상한스킵→승급가능) vs 수정후(2코트 상한6 도달로 차단 / 3코트 상한9 안에서만 승급) 시뮬레이션 대조 |
| 교훈 | "특정 조건에서 규칙을 건너뛴다"는 수정은 자칫 "규칙을 완전히 없앤다"는 결과를 낳을 수 있음 — 조건이 늘어나는 상황(코트확대 등)에서는 규칙 자체를 끄기보다 "그 상황에 맞게 규칙 값을 재계산"하는 방향이 안전함 |

## v135. "늦참 등록했는데 리스트에 안 보임" — 참석이 조용히 취소되던 버그 (김진주님 사례)

| 항목 | 내용 |
|---|---|
| 제보 | 8/13 11:40에 늦참10분 체크했는데 리스트에 안 보임 |
| 추적방법 | ignis_notifications 테이블에서 실제 알림 로그를 시각순으로 조회해 정확한 원인 특정 |
| 실제 로그 | 11:40:24 "참석체크"→"늦참(10분) 참석등록"(정상) → 3초후 11:40:27 "참석을 취소했습니다" |
| 근본원인 | checkLateArrival이 setVote(eventId,'attend',gender)를 호출하는데, setVote는 "이미 참석중인데 attend 재호출시 취소 처리"하는 토글 로직 보유. 이미 참석(늦참) 상태에서 늦참버튼 재클릭/중복클릭시 토글에 걸려 참석이 삭제됨 |
| 수정 | checkLateArrival에서 이미 attend 상태면 setVote를 호출하지 않고, 기존 참석기록에 lateArrival/lateMinutes만 직접 업데이트하도록 변경 |
| 실데이터 정정 | 김진주님 8/13 목요일밤 유니온 기록을 late_arrival=true, late_minutes=10으로 Supabase 직접 수정 |
| 검증 | 토글버그 재현(수정전 삭제됨)/수정후(정상유지) 시뮬레이션 |

## v136. 자동승급 전체 재감사 — v134 자체의 회귀버그 2건 발견/수정

| 항목 | 내용 |
|---|---|
| 요청 | 자동승급/성별승급/연참승급/관리자즉시승급 전부 엄격 재검토, 회귀오류·충돌지점까지 비판적으로 확인 |
| 회귀①(공식불일치) | v134의 genderCap이 quota(고정값) 기준이라, 미확보 구간에서 신규등록(max_players기준, 더 작음)과 승급함수(quota기준, 더 큼)가 서로 다른 상한을 씀 — 신규등록에서 대기로 밀린 사람이 20초 자동승급 주기에서 승급될 수 있는 충돌. 공식을 `Math.floor(ev.max_players/2)`로 신규등록과 통일 |
| 회귀②(범위확대) | v134에서 조건 단순화하며 `ev.court_count === 2` 제한이 실수로 사라져, 1코트·3코트이상 이벤트에도 성별상한 적용됨 — 이런 이벤트는 genderFlex가 절대 안 걸려서 영원히 안 풀리는 하드캡이 될 뻔함. `ev.court_count === 2` 조건 복원(4개 함수 중 3개 승급함수) |
| 추가점검 | setVote(attend) 호출부 전수 재확인(토글위험 추가지점 없음), late_consecutive필터 4곳 재확인, attend생성 11개지점 전수 재매핑, 불참전환/안내알림 함수 오탐 배제 |
| 검증 | 미확보구간 충돌해소/1코트면제복원/원래버그시나리오 안전 3가지 시뮬레이션 |
| 교훈 | 버그 수정 자체가 새 버그(회귀)를 만들 수 있음 — 특히 "조건을 단순화"하거나 "다른 값으로 대체"할 때는 그 변경이 원래 의도했던 범위·다른 코드와의 일관성까지 깨지 않는지 반드시 재확인 필요 |

## v137. 진짜 네 번째 원인: 연참 전용 승급경로에 성별상한 체크 누락

| 항목 | 내용 |
|---|---|
| 재제보 | v134/v136 수정 후에도 남6·여5 상태에서 대기2명이 승급될 것 같다는 문제 지속 |
| 실데이터 확인 | 대기중인 남자 2명(이경규,김영진)이 둘 다 vote_type=consecutive(연참) — tryAutoPromote 등 3곳엔 있던 성별상한 체크가 연참전용경로(checkConsecutiveWaitingLatePromotion, 시작1시간전 실행)에는 애초부터 전혀 없었음. v131부터 지금까지 이 네번째 함수만 계속 누락 |
| 수정 | checkConsecutiveWaitingLatePromotion에도 동일한 성별상한 체크(court_count===2, genderCap=floor(max_players/2), flex예외) 추가 |
| 실데이터 상태 | 다행히 아직 대기 상태로 남아있어 실제 오류발생 전에 수정함(사전 예방) |
| 검증 | 실제 라이브 데이터(남6·여5·연참대기2) 그대로 시뮬레이션 재현 → 수정후 정상 차단 확인 |
| 교훈 | 같은 "성별상한"이라는 규칙이 4개의 서로 다른 함수(경로)에 각각 구현되어 있으면, 그중 하나만 놓쳐도 전체 규칙이 무력화됨 — 이런 교차편수(cross-cutting) 규칙은 헬퍼함수로 한 곳에 모아 재사용하는 게 더 안전할 수 있음(향후 리팩터링 고려사항) |

## v138. 진짜 최종 원인: "필터 한번+순차승급" 구조적 결함 + 서버 실시간 재확인 안전장치

| 항목 | 내용 |
|---|---|
| 확인경위 | 사용자가 실제 배포버전(v137)임을 확인 — 이론적 검증만으로는 못 찾은 문제라 실제 알림로그 전체를 시간순으로 재조사(8/13 새벽 03:00~04:45, 12회 이상 반복 확인) |
| 진짜 원인 | tryAutoPromote/forcePromoteWaitingNow가 "대기목록을 성별상한으로 한 번 필터링 → 순서대로 승급"하는 구조였음. 필터링 시점 카운트로는 여러 명이 동시에 통과 가능한데, 승급 루프 안에서는 성별상한을 재확인 안 하고 관외비율만 재확인 — 한 사이클에 여러 명이 연달아 승급되며 상한 초과 가능 |
| 수정 | getLiveGenderCounts(eventId) 신설 — 로컬캐시 아닌 Supabase 실시간 조회. 4개 함수(tryAutoPromote/promoteWaitingForCapacity/checkConsecutiveWaitingLatePromotion/forcePromoteWaitingNow) 전부 실제 DB저장 직전에 이 실시간 카운트로 성별상한 최종 재확인. 조회실패시 안전하게 승급 보류 |
| 부가효과 | 여러 기기/세션 동시접속으로 인한 로컬캐시 시차(동시성) 문제까지 함께 방어 |
| 검증 | 필터통과 2명이 순차승급되어 상한초과(버그재현) vs 1명만 승급되고 정확히 상한에서 멈춤(수정후) 시뮬레이션 |
| 교훈 | "필터링 후 순차처리" 패턴에서는, 필터링 시점 스냅샷이 처리 도중 무효화될 수 있음 — 특히 여러 항목을 연속으로 쓰기(write)하는 루프에서는 매 쓰기 직전 최신 상태 재확인이 필수 |

## v139. 성별참석 수동마감(genderClosed)이 승급함수에서 전혀 지켜지지 않던 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 관리자가 성별참석 수동마감을 걸어도, 대기중인 사람이 자동승급되는지 확인 요청 |
| 확인결과 | genderClosed는 신규등록(setVote)만 대기로 돌리고, 승급함수 5개(tryAutoPromote/promoteWaitingForCapacity/checkConsecutiveWaitingLatePromotion/forcePromoteWaitingNow/promoteGenderFlexWaiting) 어디에도 체크가 전혀 없었음 |
| 수정 | 5개 함수의 필터링단계 + v138에서 추가한 실시간재확인단계 총 9곳 모두에 genderClosed 체크 추가 — 상한 여유 있어도 관리자 수동마감시 무조건 차단 |
| 예외 유지 | adminMoveMember("이동▾" 개별지정)는 genderClosed와 무관하게 정상작동 — 관리자의 특정회원 직접추가까지 막지 않도록 의도적 예외 |
| 검증 | 상한여유(5명)+수동마감=차단 vs 마감없음=정상승급 시뮬레이션 |

## v140. 성별 예외규칙 근본 재설계: 영구고정 flex → 실시간 반대성별 판정

| 항목 | 내용 |
|---|---|
| 요청 | 8/20 테스트에서 v139에도 여5·남7 발생 가능성 지적 — 정확한 규칙 재정의: 반대성별 2명이하일때만 6명초과 허용, 4명이상이면 무조건 6명 하드캡(관리자 개별추가만 예외) |
| 진짜 원인 | genderFlex(전날 낮12시 판정, 남/여로 확정되면 영구고정)를 승급함수들의 예외판정 기준으로 썼음 — 한번 반대성별 2명이하로 예외가 걸리면, 그후 반대성별이 회복돼도 예외가 절대 안 풀림(checkGenderFlexPromotions가 재판정을 스킵하도록 설계돼있었음) |
| 수정 | 8개 지점(5개함수의 필터링+실시간재확인단계) 전부 flex 필드 참조 제거, `oppositeGenderCount(Live) <= 2`를 그 순간 직접 계산해서 판정 — 반대성별 회복시 예외 즉시 해제 |
| 검증 | 반대성별4이상차단/2이하허용/3경계값차단/회복시예외해제/수동마감최우선 5가지 시나리오 시뮬레이션 |

## v141. 성별 예외기준 정교화: 2명이하 → 3명이하(복식4명미만)

| 항목 | 내용 |
|---|---|
| 요청 | 복식게임은 4명 필요 — 반대성별이 4명 미만(3명이하)이면 자체복식 불가하니 상한초과 허용, 4명이상이면 예외없음 |
| 수정 | v140의 oppositeGenderCount/oppositeLiveCount 기준을 <=2에서 <=3으로 변경(8개 지점). checkGenderFlexPromotions 트리거(2→3), 알림·UI 문구도 전부 통일 |
| 검증 | 4가지 시나리오 순차시뮬레이션: 남6여3+남5대기(3명승급) / 여6남4+여2대기(0명승급) / 남3여4+여5대기(5명전원승급) / 남1여6+여3대기(3명전원승급) |

## v142. 긴급참석 승인완료 팝업이 재접속마다 반복되던 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 박진민님이 접속할 때마다 긴급참석 요청 승인완료 팝업이 계속 뜸 |
| 원인 | checkMyAutoApprovedPopup()이 확인플래그를 로컬 메모리에서만 delete하고 서버에 저장 안 함 — 재접속시 서버 재로딩으로 플래그가 다시 true가 되어 팝업 반복 |
| 수정 | delete 대신 false로 바꾸고 saveVoteRow로 서버에도 즉시 저장. 호출부도 async 처리에 맞게 await 추가 |
| 실데이터 정리 | 박진민 포함 총 5명(이윤재,정승식,최강영,설재훈)이 동일 상태였음을 발견해 전부 Supabase에서 정리 |
| 검증 | 수정전(재발)/후(1회로 종료) 시뮬레이션 |

## v143. 성별대기·일반대기 등록기준 명확화

| 항목 | 내용 |
|---|---|
| 요청 | 전체정원(미확보시8명)이 넘어가면 성별무관 선착순 일반대기로, 그전(전체여유+내성별만꽉참)의 성별대기는 등록시각 우선순위 유지 |
| 기존문제 | 신규등록시 성별상한 체크를 먼저 하고 전체정원 여유는 확인 안 해서, "전체도 성별도 다 꽉찬" 상황에도 gender_wait으로 애매하게 분류됨 |
| 수정 | gender_wait 등록조건에 `&& !wouldBeFull` 추가 — 전체정원이 이미 찼으면 성별무관 자동으로 regular 등록. waitingSortKey는 이미 두 타입을 동급(ts만 비교)으로 처리하므로 우선순위는 그대로 유지 |
| 검증 | 3가지 등록시나리오 + 먼저등록한 gender_wait이 나중등록한 regular보다 항상 우선인지 시뮬레이션 |

## v144. 일정 삭제 실행전 관리자 비밀번호 재확인 신설

| 항목 | 내용 |
|---|---|
| 요청 | 코트 삭제는 실수로 잘못 누를 수 있으니 삭제 직전 관리자 비밀번호 재확인 요청 |
| 구현 | 기존 전역 ADMIN_PASSWORD_HASH 재사용(관리자모드 진입시와 동일 비밀번호) — 삭제옵션 선택 직후, 실제 삭제로직 실행전에 prompt로 비밀번호 확인, 불일치/취소시 삭제 자체가 취소됨 |
| 유지 | "반복일정 전체"의 기존 confirm()도 그대로 유지 — 비밀번호+확인창 이중 안전장치 |
| 검증 | 취소/오답/정답 3케이스 시뮬레이션 |

## v145. 3번코트 확보시 대기승급 순서 역전 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 성별대기(gender_wait)가 일반대기(regular)보다 먼저 등록되어 있었는데도, 3번코트 확보로 정원이 늘어나는 순간 일반대기(늦참·연참 포함)가 먼저 참석으로 승급되는 경우 발견 |
| 원인 | 3번코트 확보시 실행되는 `promoteWaitingForCapacity()`가 대기목록 중 최우선(선착순+타입우선순위) 후보 1명만 `.find()`로 골라 승급을 시도하고, 그 후보가 서버 실시간 재확인(`getLiveGenderCounts`)에서 막히면 그대로 `break`로 함수 전체를 중단시키는 구조였음 — 같은 파일의 다른 4개 승급함수(tryAutoPromote/forcePromoteWaitingNow 등)는 막힌 후보만 `continue`로 건너뛰고 계속 진행하는데, 이 함수만 예외적으로 전체중단(`break`) 방식이었음. 이 때문에 최우선 후보가 한 번 막히는 순간 그 뒤 후보(나중 등록한 일반대기 등)는 이 호출에서 아예 기회를 못 받고, 이후 별도 주기의 다른 승급함수가 먼저 그들을 승급시켜 순서가 뒤바뀜 |
| 수정 | `promoteWaitingForCapacity()`를 다른 4개 함수와 동일한 패턴(대기목록 1회 필터링+정렬 → for문으로 순회하며 막힌 후보는 continue, available 개수만큼 계속 승급)으로 재작성. 기존 20회 재시도 outer 루프 제거(더 이상 필요 없음 — 단일 패스로 안전하게 전원 처리). 선착순+타입 우선순위(일반·성별대기 동급 > 늦참 > 연참) 정렬과 성별정원 실시간 재확인 로직 자체는 변경 없음 |
| 검증 | node --check 구문검증 통과, 기존 코드와의 diff로 이 함수 외 다른 코드 변경 없음 확인 |

## v146. 1코트 이벤트 정원 초과되던 동시성 버그 수정 (8/18 유니온 저녁 사례)

| 항목 | 내용 |
|---|---|
| 제보 | 8/18 유니온 저녁(1코트, 정원6명)이 실제로는 8명까지 채워짐 |
| 실데이터 추적 | 시작1시간전 연참승급 시점(4초 사이)에 3명 거의 동시승급 — 6번째까지는 정원과 일치했는데 그 직후 2명 초과 |
| 진짜원인 | v138의 "서버 실시간 재확인" 안전장치가 court_count===2에서만 실행 — 1코트 이벤트는 이 안전장치 자체가 스킵되고 전체정원(available) 체크가 로컬캐시로만 판단됨. 여러 세션 동시접속시 각자 다른 시점 로컬캐시로 독립판단해 정원초과 가능(동시성/race condition) |
| 수정 | 4개 승급함수 전부, 코트수 무관하게 저장직전 항상 서버실시간 데이터로 전체인원 재확인하도록 통합 — getLiveGenderCounts를 모든 이벤트 공통 적용 |
| 검증 | 동시성 재현(3세션 로컬캐시로 8명까지 초과) vs 수정후(서버실시간조회로 정확히 6명 차단) 시뮬레이션 |

## v147. 참석이력에 자동승급이 우연히 로그인된 사람으로 잘못 표시되던 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 8/18 18-20시 참석 히스토리에 "전은지님이 승급시켰다"고 나오는데 전은지님은 개입한적 없음 |
| 1차확인 | 일반알림은 전은지 본인이 승급대상자였다는 정상 문구 |
| 진짜원인 | saveVoteRow()가 이력기록시 actor를 항상 state.myName(그순간 로그인된 사람)으로 기록 — 자동승급함수(20초 백그라운드타이머)도 이 함수를 호출하니, 하필 자동승급 순간 로그인해있던 사람이 "처리자"로 잘못 남음 |
| 수정 | saveVoteRow에 actorOverride 매개변수 신설, 자동실행 7개함수는 명확한 시스템라벨 전달. 관리자 실제클릭 액션은 기존대로 실명 유지 |
| 부가개선 | 이력표시 화면도 시스템actor는 자연스러운 문구로 표시하도록 개선 |
| 검증 | 수정전(오귀속)/수정후(시스템명시)/관리자실제처리(실명유지) 3가지 시뮬레이션 |

## v148. 새벽테니스 3번코트 미확보 기준 정원 6명→4명 조정

| 항목 | 내용 |
|---|---|
| 요청 | 새벽테니스 3번코트 미확보 상태 기준인원 4명, 확보시 8명(기존유지) |
| 확인 | computeCapacity()의 새벽코트 전용 공식에서 미확보 기준값(unconfirmedMax)만 6→4로 수정, 확보시 quota(8)는 변경없음 |
| 영향범위 | 새벽코트는 이미 성별상한 예외(isDawnEvent) 적용중이라 전체정원(max_players) 체크에만 영향 |
| 검증 | 미확보4명/확보8명 시뮬레이션 |

## v149. 평일 새벽테니스 전용 "레슨참여" 항목 신설

| 항목 | 내용 |
|---|---|
| 요청 | 레슨 회원이 레슨시간 등록해서 부재시간대를 다른 회원이 알 수 있게 표시(예: "서힘찬 참석 (레 06:40)") |
| DB | ignis_votes에 lesson_time 컬럼 신설 |
| UI | 늦참버튼 옆 "📚레슨" 버튼 - 평일 새벽테니스(daily_union_morning) 전용 노출 |
| 핵심설계 | 늦참과 달리 정원·대기순위엔 전혀 영향없는 순수 정보성 표시 - 이미참석중이면 시간만 추가, 미참석이면 일반참석과 동일 처리 |
| 안전패턴 | 늦참의 "이미참석중이면 setVote 재호출안함" 패턴 재사용해 토글취소버그 회피 |
| 검증 | 표시형식/버튼노출조건/시간형식검증 시뮬레이션 |

## v150. 새벽 관리자(VIP3) 새테 정원초과 참석권한 부여

| 항목 | 내용 |
|---|---|
| 요청 | 새벽 관리자들에게 새테 정원이 찼어도 운영진처럼 참여 가능한 권한 부여 |
| 확인 | 기존 운영진(staff)만 정원마감시 히든석으로 예외참석 가능했음 — 새벽테니스(isDawnEvent) 한정, VIP3(새벽관리자)에게도 동일 적용 |
| 부가조정 | 여러 명이 동시사용 가능하도록 새벽테니스 한정 히든석 인원제한 성별당 1명→3명 완화(다른 이벤트는 기존 1명 유지) |
| 범위제한 | isDawnEvent && isVip3 조합에서만 적용, 다른 유니온 일정 영향없음 |
| 검증 | 6가지 시나리오 시뮬레이션 |

## v151. 성별확대 안내배지 영구고정 표시버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 8/27 목요일 유니온, 여자4명인데 "남자12명확대적용중" 배지가 뜨는 이유 확인요청 |
| 확인 | 실제 승급판정(v140~141)은 정상작동(6명에서 정상차단), 문제는 화면 안내배지뿐 |
| 진짜원인 | checkGenderFlexPromotions()의 "남/여 확정되면 영구고정, 재검토안함" 로직이 남아있었음 — v140에서 승급게이트키핑만 실시간화하고 이 필드 갱신로직은 안고침 |
| 수정 | 영구고정 조건 제거, 매번(20초주기) 최신인원으로 재판정하도록 통일 |
| 실데이터 정정 | 8/27 목요일 genderFlex를 균형으로 즉시수정, 나머지 미래일정은 배포후 자동정상화(과거일정은 expired라 무관) |
| 검증 | 옛값박제상태->실제데이터 재판정시 균형으로 정확히 갱신 시뮬레이션 |

## v152. 3번코트 되돌릴때 초과인원 자동 대기복귀 로직 신설

| 항목 | 내용 |
|---|---|
| 발견경위 | v151 직후 유사버그(상태변경시 이전확정결과 안되돌림) 전수점검 중 발견 |
| 문제 | toggleCourt3Confirmed()가 확보시엔 자동승급하지만, 미확보로 되돌릴때는 정원만 줄고 이미 승급된 사람은 그대로 남음 |
| 수정 | demoteExcessAfterCourt3Revert() 신설 - 되돌릴때마다 전체정원·성별상한(v141과 동일기준) 위반여부 확인해 가장 나중에 승급된 사람부터 순서대로 대기전환 |
| 공정성 | 원래 대기등록시각(ts)·대기유형(연참등) 복원 - 재확보시 새치기없이 원래순번대로 재승급 |
| 예외 | 히든석(운영진/새벽관리자)은 되돌림 대상에서 제외 |
| 검증 | 확보로 승급된 3명 중 정원축소분만큼 가장최근승급자가 정확히 복귀하는 시뮬레이션 |

## v153. AI관리자 댓글 중복게시 수정 + 늦참/레슨/연참 서브박스 신설 + 레슨자 4인제한 + 관리자 직접수정 기능

### 1. AI관리자 댓글 중복게시 수정
| 항목 | 내용 |
|---|---|
| 요청 | AI관리자 댓글이 중복으로 계속 달리는 문제, 단순하게 코트당 중복댓글 안달리게 수정 |
| 확인 | ①"오늘 참석인원..." 안내가 pickVariant 끝인사만 다른채 1초 간격 3연속 게시(레이스컨디션) ②"3번코트 확보완료" 몇시간 간격 반복은 실제 관리자가 여러번 누른 정상이력(버그아님) |
| 원인① | 기존 중복판정 비교범위(30자)가 이모지 UTF-16 특성상 끝인사까지 포함 못해 서로 다르게 판정됨 |
| 수정 | 비교범위 30자→45자, 시간윈도우 30분→6시간, 조회개수 10→30개로 확대 |

### 2. 참석체크리스트 늦참/레슨/연참 서브박스 신설
| 항목 | 내용 |
|---|---|
| 요청 | 여자참석/남자참석 칸 아래에 늦참·레슨자 칸 추가해서 직관적으로, 연참 칸도 검토. 목업 여러안 제시 후 하이브리드(인라인뱃지+하단서브박스) 확정 |
| 구현 | renderAttendSubBoxes() 신설 — 늦참(lateArrival)·레슨(lessonTime)·연참(promotedFrom==consecutive) 해당자만 별도 서브박스로 표시, 해당자 없으면 박스 숨김 |
| 보완 | lateMinutes 5분등 작은값도 안전하게(!=null) 표시, 레슨 뱃지 "(레슨 06:40)"으로 명확화 |
| 확대 | 레슨버튼 daily_union_morning 전용 제한 제거 — 전체 코트 적용 |

### 3. 레슨자 4인 미만 참석제한
| 항목 | 내용 |
|---|---|
| 요청 | 4인미만시 레슨자 참여하면 복식불가하므로 관외대기처럼 4인이상일때만 참석가능+우선순위 늦참30분이상과 동급. 새벽코트제외 평일저녁6시이후 적용 |
| 구현 | checkLessonParticipation() 신규등록시 평일(토일제외)+18시이후+전체인원3명이하면 type:late(늦참30분+와 동일우선순위) 대기로 전환 |
| 핵심안전장치 | canPromoteRespectingLessonMinimum() 신설(canPromoteRespectingGwaeLimit과 동일패턴) — 승급직전 재확인 안하면 일반승급함수가 정원여유만 보고 4인미만 상태에서도 레슨자를 그냥 승급시킬 위험 있었음. 4개 승급함수+find방식 2곳 총 5곳 적용 |
| 정보유지 | lessonTime을 5개 승급지점 모두에서 승급후에도 유지하도록 수정 |

### 4. 관리자용 늦참/레슨시간 직접수정 기능
| 항목 | 내용 |
|---|---|
| 요청 | 일반참석 누르고 댓글로만 늦참 알리는 회원들 있어, 관리자가 참석상태뿐 아니라 늦참분·레슨시간도 직접 수정 가능하게 |
| 구현 | 이동▾ 드롭다운에 "늦참 시간 수정"·"레슨 시간 수정" 옵션 신설 — 참석상태 회원 한정, 분단위/시작시간 프롬프트 직접입력 |
| 부가발견 | adminMoveMember()의 서버 재조회 로직에 lessonTime 필드가 빠져있던 것 발견(v149 이후 누락) — 재조회객체와 attend처리 양쪽에 추가 |

## v154. 레슨자 4인미만 제한에 "시작 1시간전 특례" 추가

| 항목 | 내용 |
|---|---|
| 요청 | 레슨자 참석제한(4인미만시 대기)에, 연속참석처럼 시작1시간전에도 정원 안찼으면 인원부족해도 승급허용 조건 추가 |
| 구현 | checkConsecutiveWaitingLatePromotion()(기존 연참·늦참연참 전용 시작1시간전 경로) 대상필터에 레슨자대기(type:late && lessonTime)도 포함, 이 경로안에서는 canPromoteRespectingLessonMinimum 체크 의도적 생략 |
| 경로분리 | 순수 늦참(30분초과, lessonTime없음)은 이 특례 대상 아님 — 기존 일반경로로만 처리 |
| 유지 | 성별상한·관외비율·전체정원 서버실시간재확인은 변경없이 그대로 |
| 검증 | 레슨자1시간전특례승급/일반늦참대상아님/정원마감시차단 시뮬레이션 |

## v155. 지문체크/공불출/클릭충돌/버튼레이아웃 수정 + 새벽코트 남녀통합 시범운영

### 1. 지문체크 2개월 고정주기 수정
| 항목 | 내용 |
|---|---|
| 요청 | 남은횟수가 1년으로 카운트, 2개월단위(7,8/9,10월식) 고정리셋 필요 |
| 원인 | 기존 "오늘부터 2개월후까지" 롤링윈도우라 리셋없이 끝없이 카운트됨 |
| 수정 | 홀수월시작 고정 2개월주기(1-2/3-4/.../11-12월)로 변경 |

### 2. 공불출내역 충전히스토리 신설
| 항목 | 내용 |
|---|---|
| 요청 | 운영진이 공 채워넣는 수량, 누가언제몇개 표시 |
| 수정 | ballRefillHistory 신설 - 직접입력/+버튼 조정시 누가/언제/몇개/변경후재고 기록, 접이식 히스토리로 표시 |

### 3. 참석시간 클릭 충돌 수정
| 항목 | 내용 |
|---|---|
| 요청 | 참석시간(chip-ts) 눌러도 히스토리 팝업 안뜨는 경우 많음 |
| 원인 | pollForUpdates엔 있던 "클릭중이면 재렌더링건너뛰기" 보호장치가 20초 자동승급주기(runAutoPromotions) 재렌더링엔 빠져있어 클릭타이밍과 겹치면 DOM이 통째로 교체되며 클릭 씹힘 |
| 수정 | 동일 보호장치(userIsInteracting체크) 추가 |

### 4. 참석/불참 상단, 늦참/레슨/대기 하단 배치
| 항목 | 내용 |
|---|---|
| 요청 | 참석/불참 위로, 늦참/레슨/대기 아래로 배치 |
| 원인 | 기존 5개버튼 동일크기라 홀수개수로 마지막(불참)이 혼자 다음줄 차지하며 커보임 |
| 수정 | 참석+불참을 위쪽 큰줄(ev-actions-main), 늦참+레슨+대기를 아래쪽 작은줄(ev-actions-sub)로 재구성 |

### 5. 새벽코트 남녀통합 + 레슨분리 시범운영
| 항목 | 내용 |
|---|---|
| 요청 | 새벽코트만 남녀통합 표시 + 레슨자는 통합인원에서 제외해 별도표시, 훈련위주 시범운영 |
| 구현 | isDawnEvent(ev)일때만 여자/남자참석 박스 대신 통합 "참석"박스 표시, 레슨자는 renderAttendSubBoxes로 이미 분리되니 통합박스 인원수에서 제외 |
| 안전장치 | 정원(max_players)·전체인원(regularTotal) 등 실제 계산로직은 전혀 변경없음 - 순수 표시만 변경. 복잡해지면 이 표시분기만 되돌리면 원래방식으로 즉시복귀 가능 |

## v156. Supabase 저장안전성 이중점검 + 참석불참 높이통일 + 복식가능배지 삭제

### 1. Supabase 저장 안전성 이중점검
| 항목 | 내용 |
|---|---|
| lesson_time 컬럼 | 실제 존재 확인(정상) |
| ballRefillHistory·voteHistory 병합버그 | 배열(이력로그)값인데 기존병합이 dirty키 전체를 통째로 덮어써서, 두 세션 동시기록시 한쪽이 사라질 위험 - 서버+로컬 배열 ts기준 병합으로 수정 |
| saveVoteRow 실패시 조용히 삼켜지던 문제 | 함수가 true/false 리턴하도록 개선, 신규기능(레슨등록3곳+관리자수정2곳)에 저장실패 알림 추가 |
| 범위 | 기존 41곳 saveVoteRow 호출부는 안건드림(새버그유입 위험 회피), 신규기능 위주로 강화 |

### 2. 참석·불참 버튼 높이 통일 + 복식가능배지 삭제
| 항목 | 내용 |
|---|---|
| 요청 | 참석/불참 칸 너무커서 수직높이 일정하게, 4명시 뜨는 복식가능 안내배지 삭제 |
| 수정 | height:44px 고정+flex중앙정렬로 텍스트길이 무관 항상 동일높이. "복식가능·남복가능·여복가능" 배지 완전삭제 |

## v157. 가이드(메뉴얼)에 레슨·새벽테니스 운영방침 항목 신설

| 항목 | 내용 |
|---|---|
| 원칙 | 규칙변경시 가이드도 같이 업데이트(기존 개발노트 원칙 준수) |
| ⑧ 레슨등록제도 | 등록방법, 평일저녁6시이후 4인미만 대기규칙(늦참30분+와 동일우선순위), 자동승급조건 |
| 새벽코트 예외 강조 | ⑧항목 안에 강조박스로 "새벽테니스는 4인대기규칙 적용안됨" 명확히 구분표시 |
| ⑩ 새벽테니스 운영방침 신설 | 기본4명/3번코트확보시8명, 조기집합권장, 레슨자인원제외, 시범운영고지 |
| 부가 | 게스트참석 번호 ⑧→⑨ 조정, 단톡방 공지용 상세설명문서 별도작성 |

## v158. 레슨 4인미만 대기 표시가 "늦참"으로 잘못 나오던 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 8/28 18-20시 레슨19:00 테스트등록시 대기표시가 "늦참"으로 나옴 |
| 원인 | v153에서 레슨자대기를 type:late로 등록해 늦참과 동일우선순위 부여했는데, 이 type값을 화면표시에도 그대로 써서 "늦참대기" 라벨이 노출됨 - 우선순위로직과 표시라벨이 분리안됨 |
| 수정 | computedCounts()에서 type:late 대기자를 lessonTime유무로 분리, 별도목록(waitLessonNames) 생성 - 우선순위는 late와 동급유지, 표시만 "(레슨)"/"레슨 대기"로 구분 |
| 반영위치 | 카드대기명단 태그, 본인대기알림 라벨/힌트, 순수늦참은 기존유지 |
| 검증 | 제보케이스(레슨19:00) 시뮬레이션, 순수늦참 대조군 확인 |

## v159. 레슨자 승급시 "늦참 ?분 예정" 물음표 표시버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 대기승급됐는데 물음표(?) 표시가 있음, v158과 같이 해결됐는지 확인요청 |
| 안전장치 | renderMyWaitingNotice 순번계산 "?" 표시를 "순서 확인 중"으로 개선(이론상 -1 안나오지만 방어적으로) |
| 진짜원인 | 4개 승급함수의 AI관리자댓글이 전부 "늦참 N분 예정" 형태로 lateMinutes없으면 물음표 표시하는 방식 - 레슨자대기(lateMinutes없음)가 승급될때 그대로 타서 "늦참 ?분 예정" 표시됨 |
| 수정 | 4곳(성별유연승급/정원확대/일반승급/관리자즉시실행) 모두 lessonTime유무로 분기 - 레슨자는 "레슨 HH:MM 예정", 순수늦참은 기존유지 |
| 검증 | 레슨19:00승급/늦참30분승급 대조군 시뮬레이션 |

## v160. 연참·레슨 "1시간전 특례" 1회성실행 구조적결함 수정

| 항목 | 내용 |
|---|---|
| 제보 | 1시간전까지 자동승급 안되고 계속 대기로 남아있는 현상 체크요청 |
| 진짜원인 | checkConsecutiveWaitingLatePromotion()이 notifiedFlags로 "시작1시간전 딱 한번만" 실행되도록 막혀있었음 - 그 한번의 시도에서 정원없어 실패하면, 이후 자리나도 재실행 안되어 영원히 대기 |
| 수정 | 1회성 실행제한(플래그) 제거, 매번(20초주기) 조건만족하면 계속 재확인 - 이미승급된사람은 대기목록서 빠지니 중복승급 위험없음(멱등성) |
| 영향범위 | 이 결함은 순수연참(consecutive)·늦참연참(late_consecutive)에게도 이전부터 있던 문제 - 이번수정으로 함께 개선됨 |
| 검증 | 수정전(1시간전실패->영원히대기)/수정후(자리나면즉시승급) 대조 시뮬레이션 |

## v161. 레슨↔늦참 상호 덮어쓰기로 정보유실되던 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 레슨19:00으로 등록했는데 승급될때 늦참으로 인식됨 (서힘찬님 실사례) |
| 조사 | voteHistory 확인결과 waiting-attend 4차례 반복, 최종 attend레코드에 lesson_time null, late_arrival false - 승급함수보다 앞단(대기등록단계)에서 이미 정보유실 확인 |
| 진짜원인 | checkLateArrival()의 30분초과 대기등록이 votes[name]을 완전히 새 객체로 덮어씀 - 레슨대기중에 늦참추가등록시 lessonTime 통째로 사라짐(반대방향도 checkLessonParticipation에 동일버그) |
| 수정 | 양쪽함수 모두 새 객체 생성전 기존값(lessonTime 또는 lateArrival/lateMinutes) 먼저 읽어서 보존 |
| 검증 | 양방향(레슨상태+늦참추가/늦참상태+레슨추가) 시뮬레이션 |

## 참고: 대기 승급 우선순위 전체 (재확인)
일반대기·성별대기(0) > 늦참(30분+)·레슨대기(1, 동급) > 연속참석(2) > 늦참+연참동시(3, 최하위)

## v162. 게스트 초대자 표시 + 새벽코트 레슨포함 + 운영진VIP3 히든제거 + 새벽코트 정원8명단순화

### 1. 게스트 참석 목록에 초대자 이름 뱃지 표시
| 항목 | 내용 |
|---|---|
| 요청 | 게스트 초대하면 초대한 사람 이름을 참석뱃지처럼 표시 |
| 확인 | addGuest()에 invitedBy 필드 자체가 저장 안되고 있었음(알림 문구에만 언급, 실제 데이터엔 없음) |
| 수정 | invitedBy 저장 추가, 참석목록에 "(OOO님 초청)" 뱃지 신설, adminMoveMember 이동시에도 정보유지 |

### 2. 새벽코트 통합참석란, 레슨자도 총인원에 포함
| 항목 | 내용 |
|---|---|
| 요청 | 레슨자 제외하면 총인원 파악 어려우니, 일반참석처럼 참석란에 포함하고 레슨시간만 뱃지로 표시 |
| 수정 | "참석 N명, 레슨 M명 별도"에서 "참석 N명"(레슨자 포함) + 이름옆 레슨뱃지 방식으로 변경 |

### 3. 운영진·VIP3 정원초과시 히든석 방식 완전 제거
| 항목 | 내용 |
|---|---|
| 요청 | VIP3도 히든이 아닌 정원 뚫고 언제나 참석 가능하게 |
| 수정 | hidden:true 처리와 성별당 인원제한 로직 완전제거 - 일반참석자와 동일하게 명단표시, 인원제한 없이 언제나 참석가능 |
| 실데이터정정 | 9/3목요일 서힘찬님 hidden:false로 정정 |

### 4. 새벽코트 정원 8명 고정(3번코트확보버튼 폐지) + 4인이상시 코트확보 부탁알림
| 항목 | 내용 |
|---|---|
| 요청 | 정원4명→8명(3번코트확보버튼 없이 항상8명), 4명이상시 참석자전원에게 전날22시부터 팝업+댓글로 코트확보 부탁(댓글1회고정) |
| 수정 | computeCapacity() 새벽코트는 확보여부 무관 항상8명, 확보버튼 UI 숨김, 정원마감 안내문구 새벽코트 예외처리 |
| 신규함수 | checkDawnFourPlusCourt3Request(서버측 알림+댓글1회), checkDawnFourPlusPopup(클라이언트 실제팝업, 개인별1회) |
| 구분 | 기존 checkDawnCourt3Reminder(새벽참석자에게 저녁코트 확인요청)와는 별개 기능 |

### 참고: 대기목록 표시 문제 조사
9/1 이벤트 "대기자가 안보임" 제보 관련 — 배포된 v161 코드 그대로 실제 데이터를 Node.js 시뮬레이션한 결과 computedCounts()는 waitRegularNames를 정확히 계산함(waiting:1 정상). 데이터 계산 로직 자체엔 문제 없음을 확인. v162의 "3번코트확보버튼 폐지" 배포 후 재현되는지 확인 필요.

## v163. 🚨[중요] Supabase 1000행 조회제한으로 인한 참석·대기정보 무작위 누락 버그 수정

| 항목 | 내용 |
|---|---|
| 제보 | 곽선주님(9/1 화요일밤) 참석눌러도 반복적으로 이름이 사라져서 다시누름 - 이전 박미라·임유나 "사라짐" 제보도 동일원인 추정 |
| 진짜원인 | loadVotesTable()의 select('*')에 페이지네이션 없음 - Supabase(PostgREST) 기본 최대1000행 제한, 실제 ignis_votes가 이미 1,008행으로 한도초과 |
| 메커니즘 | 정렬기준없이 조회하다보니 매 20초폴링마다 어떤 8개행이 빠질지 무작위로 달라짐 - 그 8개에 포함된 회원기록이 그 순간 화면에서 사라졌다가 다음폴링에 다시 나타남 |
| 영향범위 | 특정회원 문제가 아니라 테이블 전체에서 무작위로 발생가능한 구조적 문제, 데이터 계속 쌓일수록 악화 |
| 수정 | loadVotesTable()에 .range() 페이지네이션 추가(1000행씩 나눠 전체조회). loadCommentsTable()(701행, 계속증가중)에도 예방적으로 동일적용 |
| 검증 | 실제 1,008행 데이터로 시뮬레이션 - 수정전 8개누락/수정후 전체 정확히 조회 확인 |

## v164. 관리자이동시 대기순번유실 수정 + 대기목록 통합연속번호 정렬

### 1. 관리자가 대기자를 참석으로 이동시킬 때 원래 등록순번 유실 수정
| 항목 | 내용 |
|---|---|
| 제보 | 9/1 화요일밤, 3번코트 확보→되돌리기→다시확보 반복후 대기·참석 순서 이상해짐 |
| 진짜원인 | adminMoveMember()의 "참석으로" 이동처리가 항상 ts:Date.now()로 새로 부여 - 원래 대기중이던 사람의 등록순번(선착순우선권)이 사라짐 |
| 수정 | 이동직전 상태가 waiting이었으면 원래 ts 보존, 그외(신규/불참)는 지금시각 사용 |
| 검증 | 이수진님 실제케이스(8/29대기등록→관리자수동이동)로 시뮬레이션, 원래순번 정확히 보존됨 확인 |

### 2. 대기목록 유형별 번호가 각자 1번부터 다시시작하던 문제 수정
| 항목 | 내용 |
|---|---|
| 제보 | 성별대기·일반대기·연속참석대기도 모두 선착순 정렬해달라, 같은1번이 여러개 보임 |
| 원인 | 일반/늦참/레슨/연속/성별대기가 각각 별도배열로 renderNameChips 따로호출되어 카테고리마다 번호가 독립적으로 재시작 |
| 수정 | renderNameChips에 startIdx파라미터 추가, renderCombinedWaitingList() 신설 - 5가지 대기유형을 waitingSortKey와 동일 우선순위(일반·성별0 > 늦참·레슨1 > 연속2)로 통합정렬후 연속번호 부여 |
| 부가 | renderMyWaitingNotice(본인대기순번안내)도 동일하게 통합순서 기준으로 정확히 계산하도록 수정 |
| 범위제외 | 관외대기·승인대기·선마감대기는 별도조건부 승급이라 통합대상에서 제외, 기존처럼 별도표시 유지 |
| 검증 | 성별/일반/늦참/연속 섞인 상황 시뮬레이션 - 우선순위+선착순 기준 1,2,3,4번 정확히 부여 확인 |

## v165. 🚨[중요] 대기순번 크래시버그 수정 + 대진표 실시간연동 + 파일 197KB 최적화

### 1. [최우선] renderMyWaitingNotice late_consecutive 크래시로 코트목록 전체 안뜨던 버그
| 항목 | 내용 |
|---|---|
| 제보 | 주소연 회원이 참석하려는데 참석 코트 목록 자체가 안뜬다는 제보 |
| 진짜원인 | v164에서 만든 "대기목록 통합" 리팩터링시, late_consecutive(늦참+연참 동시, 최하위 우선순위)를 통합처리 대상목록에서 빠뜨림 - 이 타입 대기자가 있으면 info.arr가 undefined가 되어 .findIndex() 호출시 크래시 |
| 파급효과 | renderEventList() 전체가 하나의 try-catch로 묶여있어, 이 카드 하나의 렌더링실패가 그 이후 모든 이벤트카드까지 통째로 멈춤 - "코트목록 자체가 안뜨는" 현상으로 이어짐 |
| 수정 | isCombinedType 체크목록에 late_consecutive 추가, 우선순위3(최하위)로 정확히 분리계산 |
| 검증 | 실제 주소연님 데이터로 Node.js 재현 - 수정전 크래시/수정후 정상렌더링 확인 |

### 2. 대진표 실시간 연동 버그 수정 (외부 감사리포트 검토 결과)
| 항목 | 내용 |
|---|---|
| 발견 | 대진표(bracket) 모듈이 실시간 getActiveMembers() 대신 정적 원본배열(MEMBERS)을 3곳에서 직접 참조 - 신규회원 안뜨고 탈퇴회원 계속 남는 버그 |
| 수정 | 3곳 모두 getActiveMembers()로 교체 |
| 부가 | getActiveMembers() 자체에 이름 중복제거(dedupe) 방어로직 추가 - 회원데이터에 같은이름 중복등록되어도 화면엔 항상 1명만 표시 |

### 3. 대진표를 독립 Vercel 페이지로 분리
| 항목 | 내용 |
|---|---|
| 배포 | ignisapp 저장소의 /bracket 경로에 독립 페이지로 분리배포 |
| 실시간연동 | 메인앱과 동일 Supabase DB에서 신규가입·탈퇴 정보 직접조회, 회원목록 실시간 동기화 |
| 검증 | 헤드리스 브라우저로 실제 렌더링+대진표생성 기능까지 정상작동 확인 |

### 4. 파일크기 최적화 (총 197KB 절약)
| 항목 | 내용 |
|---|---|
| Base64 이미지 중복제거 | 파비콘·공아이콘에 동일한 90KB 이미지 중복 - 런타임에 파비콘값 복사하는 방식으로 절약 |
| SITE_LOG 완전제거 | HTML내 1,333줄 개발이력 데이터+렌더링함수 완전삭제(104KB) - 앱화면 노출 안한지 오래고 RULES.md·업데이트보고서로 대체 |
| 회원화면 "업데이트" 버튼/패널 삭제 | 요청에 따라 웹사이트에서 개발이력 노출 완전제거 |

### 5. heartbeatPresence 인터벌 중복등록 수정
| 항목 | 내용 |
|---|---|
| 원인 | 로그인상태 접속시와 이름선택/변경시 각각 별도로 setInterval 등록 - 이름 재선택시 인터벌 중첩누적 위험 |
| 수정 | 전역 타이머(presenceTimer) 관리하는 startHeartbeatPresence()로 통일, 기존타이머 정리후 재등록 |

### 참고: Supabase 데이터로딩 개선방향 (다음 논의)
- 이미 ignis_votes에 realtime 구독 존재 - 20초 전체재조회 주기를 늘리고 realtime을 주력으로 전환 검토
- 날짜필터링(최근2주+미래이벤트만 조회)로 폐이지네이션 부담 자체를 줄이는 방향 우선 추천

## v166. 라이트/다크 버튼 옆에 "심플" 버전 이동 링크 추가

| 항목 | 내용 |
|---|---|
| 배경 | 다른 대화에서 제작한 심플 버전(별도 배포)을 확인 완료 후, 메인 사이트에서 그리로 이동하는 버튼 병합 요청 |
| 검증 | 업로드된 admin.html이 v165(오늘자 크래시버그수정 포함 최신본) 기준 딱 한줄(심플버튼) 차이만 있음을 diff로 정확히 확인 |
| 반영 | 라이트/다크 버튼 바로 옆에 "🎨 심플" 링크버튼 추가, href="/index.html"(dothome 내 상대경로) |
| 표준검증 | node -c 구문검증, ID매칭, MEMBERS(63명), 중복함수, DOCTYPE/html태그 전체 통과, late_consecutive크래시수정 등 기존 수정사항 유지 확인 |
| 참고 | 함께 업로드된 index_2.html은 동일 버튼이 절대URL(Vercel)로 되어있어 dothome용(admin.html)과 다름 - 어느 배포처인지에 따라 구분 사용 필요, 자세한 내용은 채팅 답변 참고 |

## v167. 코트나이트 테마 이식 + 심플버전 iframe 스킨 전환 방식으로 변경

### 배경
다른 대화에서 ① "코트나이트"(라임/골드 색상, Bebas Neue 폰트) CSS 테마 ② 완전히 새 코드베이스로 재작성된 "심플 버전"(index_2.html, APP_VERSION IGNISAPP.V3) 두 가지를 제작. 이를 메인 앱에 반영 요청.

### 중요 발견: 업로드된 admin.html이 v164 기준이었음
| 항목 | 내용 |
|---|---|
| 문제 | 업로드된 admin.html(코트나이트 포함)을 diff한 결과 v.164 기준으로 확인됨 - v165의 late_consecutive 크래시수정, 대진표 dedupe, heartbeat수정, SITE_LOG제거, 이미지최적화가 전부 빠져있었음 |
| 대응 | admin.html을 그대로 베이스로 쓰지 않고, 최신 v166(모든 버그수정 포함) 위에 코트나이트 CSS만 정확히 이식하는 방식으로 안전하게 처리 |

### 1. 코트나이트 테마 이식
| 항목 | 내용 |
|---|---|
| 반영 | Bebas Neue 구글폰트, 코트나이트 CSS 변수블록(라임#C8F169 포인트, 다크배경), 대비보정 스타일 전체 정확히 이식 |
| 버튼 | 라이트·다크 옆에 "🎾 코트나이트" 버튼 신설, applyTheme()에 courtnight 케이스 추가 |
| 기본값 | localStorage 저장값 없을때 기본 테마를 courtnight로 설정(업로드본과 동일하게) |

### 2. 심플버전을 "페이지이동 링크"에서 "iframe 스킨전환"으로 변경
| 항목 | 내용 |
|---|---|
| 요청배경 | "주소를 이동하지 말고 스킨 자체를 사이트에 녹이면 안되나?" 요청 |
| 확인 | index_2.html이 완전히 다른 코드베이스(다른 CSS변수, 다른 JS상태관리)로 재작성되어 있어 "완전 통합"은 대규모 리팩터링 필요 - iframe 방식으로 진행하기로 합의 |
| 구현 | "🎨 심플" 버튼 클릭시 전체화면 iframe 오버레이로 심플버전 표시, "✕ 일반화면으로" 버튼으로 복귀 |
| 로그인공유 확인 | 심플버전도 동일한 localStorage 키('ignis-my-name')를 사용함을 확인 - 같은 도메인(dothome)에서 서빙시 재로그인 없이 자동 로그인 상태 유지됨 |
| 배포 필요사항 | index_2.html을 dothome에 별도 파일로 업로드 필요 (현재 코드는 경로를 /simple.html로 가정 - 실제 업로드 경로에 맞게 조정 필요) |

### 검증
- node -c 구문검증, ID매칭, MEMBERS(63명), 중복함수 전체 통과
- late_consecutive 크래시수정·dedupe·presenceTimer·SITE_LOG제거·이미지최적화 등 v165/v166 수정사항 전부 유지 확인
- 헤드리스 브라우저로 실제 렌더링 - 코트나이트/라이트 테마 전환, 심플 오버레이 열기/닫기 정상 작동 확인

## v168. 심플버전 호스팅 방식 대전환 — dothome(닷홈빌더) 아키텍처 제약 발견 및 Vercel로 재배포

### 문제 발견
| 항목 | 내용 |
|---|---|
| 제보 | 심플버튼 누르면 "찾을수 없다" 화면만 뜸 |
| 진짜원인 | dothome이 전통적 파일호스팅이 아니라 "닷홈빌더" 서비스 - 관리자페이지의 "공통코드입력"(헤더/바디/푸터)이 사이트 전체에 단 하나만 적용되는 구조. "새 페이지 만들기" 같은 별도URL 페이지 개념 자체가 없어, /simple.html처럼 독립된 경로에 파일을 둘 방법이 원천적으로 없었음 |
| 확인방법 | 실제 사이트 라이브 접속 및 헤더 확인 - 메인(/)이 CodeIgniter 세션쿠키를 발급하는 닷홈빌더 자체 템플릿이었고, /simple.html은 상태코드는 200이지만 실제로는 닷홈빌더의 "페이지를 찾을 수 없음" 안내문구였음(dothome이 404를 200으로 서빙) |

### 해결책 — 심플버전을 Vercel(ignisapp 저장소)의 /simple 경로로 이전
| 항목 | 내용 |
|---|---|
| 배포 | 오늘 대진표(/bracket)를 배포했던 동일한 방식으로, ignisapp 저장소의 /simple 경로에 심플버전 배포 → https://ignisapp-zeta.vercel.app/simple |
| 로그인 연동 | dothome과 Vercel은 서로 다른 도메인이라 localStorage 자동공유 불가 - 대신 메인앱이 iframe src에 ?name=로그인한이름 을 URL파라미터로 붙여서 넘기고, 심플버전이 이 파라미터를 우선 적용해 자동로그인하도록 구현 |
| 검증 | 실제 배포된 Vercel 페이지에 이름파라미터 붙여서 접속 → 자동로그인 및 실제 참석데이터 정상표시 확인(스크린샷 확인), 메인앱에서 심플버튼 클릭시 iframe src에 이름파라미터가 정확히 인코딩되어 붙는 것까지 확인 |
| 표준검증 | node -c 구문검증, ID매칭, MEMBERS(63명), 중복함수 전체 통과 |

## v169. 게스트 초청뱃지가 새로고침 후 항상 사라지던 구조적 버그 수정

### 제보 및 원인 (제보자 직접 정확히 진단·공유해주심)
| 항목 | 내용 |
|---|---|
| 증상 | 게스트 초청 등록 직후엔 "OOO님 초청" 뱃지가 보이다가, 새로고침이나 20초 폴링 이후엔 뱃지가 사라짐. 실사례: 9/4 유니온 20-22시(fri_union_night_2026-09-04) 게스트 나나김(게스트·3년), 초청자 전은지 |
| 데이터 확인 | ignis_votes 테이블에 invited_by:'전은지'는 정확히 저장되어 있음 - 데이터 손실 아님 |
| 진짜원인 | 뱃지 표시조건이 (e.isGuest && e.invitedBy)였는데, isGuest는 게스트 추가 순간 화면 메모리에만 존재하고 saveVoteRow() upsert 목록에 컬럼 자체가 없어 서버에 전혀 저장되지 않는 값이었음. 그래서 데이터를 한 번이라도 다시 불러오면 isGuest가 항상 false로 리셋되어 뱃지가 사라짐 |
| 수정 | 조건을 e.invitedBy만 보도록 단순화(1줄). 초청코트는 computedCounts()에서 이 배열 도달 전 이미 별도분리되어 오표시 위험 없음을 확인. DB 스키마 변경 불필요, 기존에 이미 저장된 과거 게스트들의 초청자 표시도 즉시 복구됨 |
| 검증 | 실제 재현스크립트로 "새로고침 후"(isGuest 필드 자체가 없는) 상황을 시뮬레이션 - 수정후 뱃지 정상표시 확인 |
